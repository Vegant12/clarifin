---
phase: 08-ai-score-drill-down
verified: 2026-05-19T14:01:16Z
status: human_needed
score: 7/7
overrides_applied: 0
human_verification:
  - test: "Open /doc/[id] after analyze-batch has run and confirm score card renders"
    expected: "1-10 score visible in 48px emerald, 'AI Assessment · not financial advice' directly beneath, 4 dimension rows with [N/10] chips; clicking a dimension expands snippets with [p.N] citations; clicking [p.N] scrolls PDF viewer"
    why_human: "Cannot render a Next.js RSC + Radix Accordion in a headless grep-based check; full interactive behavior (accordion expand/collapse, PDF scroll callback, citation popover) requires a running browser"
  - test: "Set score_breakdown = null in Supabase for a ready document, refresh /doc/[id]"
    expected: "Muted box shows 'AI Assessment unavailable' heading and body text; 5 explanation sections still render below"
    why_human: "Null-score fallback path requires live Supabase + browser render"
  - test: "Mobile viewport (375px): explanation tab shows score card; accordion works on tap; no horizontal overflow"
    expected: "Score card fills tab width; accordion toggles on touch; no scrollbar"
    why_human: "Responsive layout and touch interaction require a real viewport"
  - test: "Keyboard accessibility: Tab focuses accordion triggers (emerald focus ring); Enter/Space toggles; Arrow keys navigate"
    expected: "Focus ring visible; keyboard navigation functional per Radix Accordion spec"
    why_human: "Focus-ring rendering and ARIA keyboard events require browser devtools"
---

# Phase 8: AI Score & Drill-Down Verification Report

**Phase Goal:** Deliver the AI-generated 1–10 holistic score with 4-dimension drill-down and citation links in the document reader UI.
**Verified:** 2026-05-19T14:01:16Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After the explanation loads, a 1-10 AI Assessment score appears with the visible label "AI Assessment · not financial advice" adjacent to the number | VERIFIED | `score-card.tsx` line 38: exact string present; score number at line 35 with same parent `<div>` (confirmed by test SCORE-06 disclaimer adjacency) |
| 2 | Score UI shows 4 sub-dimension scores with one-sentence reasoning summary | VERIFIED | `scoreSchema` enforces `dimensions.length(4)`; `score-card.tsx` maps `score.dimensions` in Accordion; test SCORE-02 "renders 4 dimension names" passes |
| 3 | Clicking a dimension expands to show 2–3 quoted snippets with page citations | VERIFIED | `AccordionContent` maps `dim.snippets` with `<CitationInline page={snip.page} ...>`; test SCORE-04 "clicking a dimension reveals snippet text" passes; `type="single" collapsible` enforces one-open-at-a-time |
| 4 | Schema validation failure triggers retry (not broken UI) | VERIFIED | `analyze-document-batch.ts` lines 188–208: ZodError retry loop (`attempt <= 2`); non-Zod errors break after attempt 1; score soft-fails to null; document still transitions to `ready` (step 9 after score block) |
| 5 | generateScore() produces a 1-10 integer overall_score | VERIFIED | `generate-score.ts` calls `scoreSchema.parse()`; Gemini called with `SCORE_MODEL_ID = "gemini-2.5-flash"` and `thinkingBudget: 0`; 8 tests pass |
| 6 | Compliance guardrail blocks buy/sell/recommend language before DB write | VERIFIED | `scanForInvestmentAdvice` runs on all `dimension.reasoning` and `snippet.text` before return; throws "Compliance violation" error; caught by retry loop; 14 tests pass incl. word-boundary edge cases |
| 7 | Full TypeScript type chain compiles without errors | VERIFIED | `npx tsc --noEmit` exits 0; `score: ScoreResult \| null` threads through `page.tsx → DocumentProgressView → DocumentReaderLayout → {DesktopSplitPane, MobileTabView} → ExplanationPanel` |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/ui/accordion.tsx` | shadcn Accordion / AccordionItem / AccordionTrigger / AccordionContent exports | VERIFIED | Generated by pnpm dlx shadcn; line 66: exports all 4 primitives |
| `src/lib/explain/score-schema.ts` | scoreSchema + SCORE_RESPONSE_SCHEMA + ScoreResult | VERIFIED | 3 exports; `z.number().int().min(1).max(10)`, `dimensions.length(4)`, `minItems: 4`, `maxItems: 3` all present |
| `src/lib/explain/score-prompts.ts` | SCORE_MODEL_ID + buildScorePrompt + scanForInvestmentAdvice | VERIFIED | SCORE_MODEL_ID = "gemini-2.5-flash"; compliance regex `\b(buy|sell|invest|...)\b/i` at line 50 |
| `src/lib/explain/generate-score.ts` | generateScore() — Gemini Files API structured score extraction | VERIFIED | imports server-only; waitForFileReady, uploadFresh, scoreSchema.parse, scanForInvestmentAdvice, thinkingBudget: 0 all present |
| `src/lib/ingest/analyze-document-batch.ts` | Extended runAnalyzeBatch with Step 8b | VERIFIED | import generateScore at line 5; score block lines 178–231; ZodError retry; onConflict: "doc_id" twice; score before status→ready |
| `src/components/doc/score-card.tsx` | ScoreCard client component | VERIFIED | "use client"; disclaimer text; text-5xl; type="single" collapsible; CitationInline wired |
| `src/components/doc/score-loading-skeleton.tsx` | ScoreLoadingSkeleton client component | VERIFIED | "use client"; animate-pulse blocks; role="status" aria-busy="true" |
| `src/components/doc/explanation-panel.tsx` | Extended ExplanationPanel with score prop | VERIFIED | `score: ScoreResult \| null` prop; ScoreCard rendered when non-null; "AI Assessment unavailable" muted fallback |
| `src/app/doc/[documentId]/page.tsx` | RSC fetches score_breakdown, safeParse via scoreSchema | VERIFIED | `.select("explanation, score_breakdown")`; `scoreSchema.safeParse`; `score={score}` passed to DocumentProgressView |
| `src/lib/explain/__tests__/score-schema.test.ts` | Real assertions, 0 it.todo | VERIFIED | 13 tests passing; 0 it.todo |
| `src/lib/explain/__tests__/score-prompts.test.ts` | Real assertions, 0 it.todo | VERIFIED | 14 tests passing; 0 it.todo |
| `src/lib/explain/__tests__/generate-score.test.ts` | Real assertions, 0 it.todo | VERIFIED | 8 tests passing; 0 it.todo |
| `src/components/doc/__tests__/score-card.test.tsx` | Real assertions, 0 it.todo | VERIFIED | 6 tests passing; 0 it.todo |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `generate-score.ts` | `generate-explanation.ts` | `import { waitForFileReady, uploadFresh, isIndonesianDoc }` | WIRED | Line 6: import confirmed; both helpers exported at lines 21/47 of generate-explanation.ts |
| `generate-score.ts` | `score-schema.ts` | `import { scoreSchema, SCORE_RESPONSE_SCHEMA, type ScoreResult }` | WIRED | Lines 7–11: import confirmed; scoreSchema.parse called at line 96 |
| `generate-score.ts` | `score-prompts.ts` | `import { SCORE_MODEL_ID, buildScorePrompt, scanForInvestmentAdvice }` | WIRED | Lines 12–16: import confirmed; all three used in function body |
| `analyze-document-batch.ts` | `generate-score.ts` | `import { generateScore }` | WIRED | Line 5; called in Step 8b loop |
| `analyze-document-batch.ts` | `document_analysis` DB | `upsert score, score_breakdown, score_at` via `onConflict: "doc_id"` | WIRED | Lines 215–225: upsert confirmed; score_breakdown at line 220 |
| `score-card.tsx` | `accordion.tsx` | `import { Accordion, AccordionItem, AccordionTrigger, AccordionContent }` | WIRED | Line 4–8: import confirmed from `@/components/ui/accordion` |
| `score-card.tsx` | `citation-inline.tsx` | `<CitationInline page={snip.page} docId={documentId} onGoToPage={onGoToPage} />` | WIRED | Line 12: import; lines 65–69: usage in AccordionContent |
| `page.tsx` | `document_analysis.score_breakdown` | `.select("explanation, score_breakdown")` | WIRED | Line 16 confirmed |
| `page.tsx` | `scoreSchema.safeParse` | `scoreSchema.safeParse(analysisRes.data?.score_breakdown)` | WIRED | Lines 27–30 confirmed |
| `page.tsx` → `DocumentProgressView` → `DocumentReaderLayout` → `ExplanationPanel` | score prop chain | `score: ScoreResult \| null` at each layer | WIRED | All 4 components verified: document-progress-view.tsx line 23/49; document-reader-layout.tsx line 32/63/120; mobile-tab-view.tsx line 21/50; explanation-panel.tsx line 82/94 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `score-card.tsx` | `score: ScoreResult` | `page.tsx` RSC → `scoreSchema.safeParse(analysisRes.data.score_breakdown)` | Yes — DB query `.select("explanation, score_breakdown")` feeds real DB data; `scoreSchema.safeParse` validates; passed down prop chain | FLOWING |
| `analyze-document-batch.ts` Step 8b | `scoreGenResult` | `generateScore()` → Gemini Files API → `scoreSchema.parse()` → upsert | Yes — generates from real Gemini API call; upserts to `document_analysis.score_breakdown` | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 41 phase-8 tests pass | `npx vitest run score-schema.test.ts score-prompts.test.ts generate-score.test.ts score-card.test.tsx` | 41 passed, 4 files | PASS |
| TypeScript compiles clean | `npx tsc --noEmit` | 0 errors | PASS |
| score-card.tsx starts with "use client" | `head -1 score-card.tsx` | `"use client";` | PASS |
| generate-score.ts starts with `import "server-only"` | `head -2 generate-score.ts` | `import "server-only";` on line 1 | PASS |
| Disclaimer exact string present in score-card | Python unicode check | `AI Assessment · not financial advice` present (U+00B7 middle dot) | PASS |
| Score block precedes status→ready in batch | `awk` trace | score_breakdown at line 220; `status: "ready"` at line 236 | PASS |
| Full interactive UI render | Requires running dev server + browser | Not tested | SKIP |

---

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| SCORE-01 | 08-01, 08-02, 08-03, 08-04 | System generates a holistic 1-10 AI assessment score | VERIFIED | `scoreSchema` enforces int 1-10; `generateScore` calls Gemini with SCORE_RESPONSE_SCHEMA; 8 generate-score tests + 13 schema tests pass; RSC surfaces score in UI |
| SCORE-02 | 08-02, 08-03, 08-04 | 4 dimensions: Profitability, Balance Sheet, Growth Trend, Valuation Context | VERIFIED | `scoreSchema`: `dimensions.length(4)`; `buildScorePrompt` lists all 4 criteria; score-card.tsx maps all 4; test "renders 4 dimension names" passes |
| SCORE-03 | 08-02, 08-03 | Each dimension includes reasoning grounded in document content | VERIFIED | `buildScorePrompt` generates per-dimension reasoning instructions; `generateScore` feeds the PDF via Gemini Files API; compliance guard scans reasoning |
| SCORE-04 | 08-02, 08-03, 08-04 | User can drill into each dimension to see 2–3 quoted snippets with page citations | VERIFIED | `snippetSchema` min(1)/max(3); Accordion AccordionContent maps `dim.snippets` with CitationInline; test "clicking a dimension reveals snippet text" passes |
| SCORE-05 | 08-02, 08-03 | Score output is schema-validated JSON (structured-output, not free-form) | VERIFIED | `SCORE_RESPONSE_SCHEMA` passed to Gemini `config.responseSchema`; `scoreSchema.parse()` validates before return; ZodError retry in analyze-batch; 14 compliance + 8 generate tests pass |
| SCORE-06 | 08-04 | Score prominently labeled "AI Assessment · not financial advice" adjacent to number | VERIFIED | Disclaimer hardcoded in score-card.tsx line 38; same `<div>` parent as score number (line 35); test "disclaimer is in the same parent block as the score number" passes |

All 6 requirements verified. No orphaned requirements found — REQUIREMENTS.md maps SCORE-01 through SCORE-06 to Phase 8 only.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

All test files have 0 `it.todo` remaining. No TODO/FIXME/placeholder comments in production files. No empty handler stubs. No hardcoded empty data flowing to rendering.

---

### Human Verification Required

#### 1. Score Card Renders Correctly on a Real Document

**Test:** Run `npm run dev`, navigate to a `/doc/[id]` where `analyze-batch` has completed (score non-null in DB). Inspect the explanation panel.
**Expected:**
- Score card renders ABOVE the 5 explanation sections
- Overall score (1-10 integer) at ~48px in emerald-600 color
- "AI Assessment · not financial advice" directly beneath the number in muted text
- 4 dimension rows (Profitability, Balance Sheet, Growth Trend, Valuation Context), each with [N/10] chip
- Clicking a row expands it; 1-3 snippet pull-quotes appear with italic text and [p.N] badges
- Clicking a second row collapses the first (type="single" behavior)
- Clicking a [p.N] badge scrolls the PDF viewer to that page

**Why human:** Cannot render a Next.js RSC with Radix Accordion in a headless file-check. Full interactive behavior (accordion expand/collapse, PDF scroll callback, popover) requires a live browser.

#### 2. Null Score Fallback State

**Test:** In Supabase SQL Editor: `UPDATE document_analysis SET score = null, score_breakdown = null WHERE doc_id = '<id>'`. Refresh `/doc/[id]`.
**Expected:**
- No score number, no dimension rows
- Muted box with heading "AI Assessment unavailable" and body "The AI assessment could not be generated for this document. The explanation below is still available."
- 5 explanation sections still visible below

**Why human:** Null-score rendering path requires live Supabase + browser — cannot be tested without a running server.

#### 3. Mobile Layout (375px Viewport)

**Test:** Resize browser to 375px wide or use device emulation. Open `/doc/[id]`, navigate to "Explanation" tab.
**Expected:**
- Score card fills full-width in the explanation tab
- Accordion opens/closes on tap
- No horizontal overflow or broken layout

**Why human:** Responsive CSS and touch interaction require a real viewport.

#### 4. Keyboard Accessibility

**Test:** Tab to accordion triggers, press Enter/Space to toggle, Arrow keys to navigate between triggers.
**Expected:** Emerald focus ring visible; keyboard navigation per Radix Accordion spec.
**Why human:** Focus-ring rendering and ARIA keyboard events require browser devtools.

---

### Gaps Summary

No programmatic gaps found. All 7 observable truths are VERIFIED, all artifacts are substantive and wired, data flows from DB through the full component chain, 41 tests pass, and TypeScript compiles clean.

Phase requires human visual verification (Task 4 checkpoint from 08-04-PLAN.md) before status can advance to `passed`. This is by design — the plan marks `autonomous: false` and includes a blocking human-verify checkpoint.

---

_Verified: 2026-05-19T14:01:16Z_
_Verifier: Claude (gsd-verifier)_
