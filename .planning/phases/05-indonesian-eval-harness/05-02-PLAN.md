---
phase: 05-indonesian-eval-harness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json
  - eval/fixtures/mid-cap-quarterly-filing-id.json
  - eval/fixtures/long-form-annual-200p-plus.json
  - eval/fixtures/mid-cap-annual-manufacturing-id-digital.json
  - eval/fixtures/goto-class-small-cap-quarterly-pack.json
  - eval/fixtures/idx-mining-annual-heavy-tables-id.json
  - eval/README.md
autonomous: false
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
tags: [eval, fixtures, idx, indonesian, gemini, gate]

must_haves:
  truths:
    - "All 9 fixture files in `eval/fixtures/` have `fixtureStatus: \"ready\"` with non-empty `numericExpectations` and `citationExpectations`."
    - "All 9 PDFs are physically present in `eval/pdfs/` with the exact filenames declared in `eval/manifest.json`."
    - "Running `pnpm eval` against the full 9-document corpus exits 0 with overall numeric ≥90% AND overall citation ≥90%."
    - "Running `EVAL_PROMPT_VARIANT=broken pnpm eval` exits non-zero with sub-threshold percentages, proving the gate is live and not decorative."
    - "Phase 6 may begin implementation — the eval gate is now operational."
  artifacts:
    - path: "eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json"
      provides: "BJBR 2023 audited ground truth — second bilingual large-cap"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/fixtures/mid-cap-quarterly-filing-id.json"
      provides: "SMGR Q3 2023 quarterly ground truth — 9-month YTD figures, period-confusion risk"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/fixtures/long-form-annual-200p-plus.json"
      provides: "ASII 2023 audited ground truth — 200+ page conglomerate, segment-substitution risk"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/fixtures/mid-cap-annual-manufacturing-id-digital.json"
      provides: "INDF 2023 audited ground truth — manufacturing consolidation scope risk"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/fixtures/goto-class-small-cap-quarterly-pack.json"
      provides: "GOTO 2023 audited ground truth — net loss (negative valueIDR variant)"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/fixtures/idx-mining-annual-heavy-tables-id.json"
      provides: "PTBA 2023 audited ground truth — heavy commodity tables, denomination-scale risk"
      contains: '"fixtureStatus": "ready"'
    - path: "eval/README.md"
      provides: "Wave 2 sign-off block — full-corpus baseline + broken-prompt regression proof"
      contains: "Wave 2 gate live"
  key_links:
    - from: "eval/fixtures/*.json (all 6 newly-ready)"
      to: "scripts/eval/run.ts aggregate scorer"
      via: "fixtureStatus === 'ready' allows extraction → scoreDocument → aggregateScores"
      pattern: "fixtureStatus.*ready"
    - from: "EVAL_PROMPT_VARIANT=broken env var"
      to: "PROMPT_EVAL_BROKEN injection in extractEvalClaims()"
      via: "src/lib/eval/gemini-eval-extract.ts reads promptVariant param"
      pattern: "promptVariant.*broken"
    - from: "process.exit(1) in run.ts"
      to: "Phase 6 gate enforcement"
      via: "shell exit code consumed by checker / CI"
      pattern: "process\\.exit\\(1\\)"
---

<objective>
Curate the six remaining placeholder fixtures (BJBR, SMGR-Q3, ASII, INDF, GOTO, PTBA) against their audited PDFs, run the full 9-document corpus through `pnpm eval` until it exits 0 at ≥90% on both numeric and citation aggregates, and prove the gate is non-decorative by running the broken-prompt variant and confirming it exits non-zero.

Purpose: Flip the eval harness aggregate exit code from red to green and lock the broken-prompt regression proof. This is the Phase 6 gate. After this plan ships, the AI Explanation Generation phase may merge only when `pnpm eval` continues to exit 0.

Output: 6 newly-`ready` fixtures (the JSON shells already exist; their `numericExpectations` and `citationExpectations` arrays must be populated from audited PDFs), 6 PDFs physically dropped into `eval/pdfs/` by the developer, a green full-corpus `pnpm eval` run, a red broken-prompt run captured as regression proof, and a Wave-2 sign-off appended to `eval/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-indonesian-eval-harness/05-RESEARCH.md
@.planning/phases/05-indonesian-eval-harness/05-PATTERNS.md
@.planning/phases/05-indonesian-eval-harness/05-AI-SPEC.md
@.planning/phases/05-indonesian-eval-harness/05-VALIDATION.md
@.planning/phases/05-indonesian-eval-harness/05-01-SUMMARY.md

@eval/manifest.json
@eval/README.md
@eval/fixtures/bbca-ar-bilingual-large-cap-digital.json
@eval/fixtures/tlkm-ar-id-only-mid-cap-digital.json
@eval/fixtures/small-cap-scanned-annual.json
@eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json
@eval/fixtures/mid-cap-quarterly-filing-id.json
@eval/fixtures/long-form-annual-200p-plus.json
@eval/fixtures/mid-cap-annual-manufacturing-id-digital.json
@eval/fixtures/goto-class-small-cap-quarterly-pack.json
@eval/fixtures/idx-mining-annual-heavy-tables-id.json
@scripts/eval/run.ts
@src/lib/eval/score-run.ts
@src/lib/eval/prompts.ts
@src/lib/eval/gemini-eval-extract.ts

<interfaces>
<!-- Slot → real company mapping (from RESEARCH.md §"Standard Corpus Slot Map") -->

| Manifest id | Company | Year | Document type | tolerancePct |
|-------------|---------|------|---------------|--------------|
| bbcj-ar-variant-bilingual-large-cap-digital-b | BJBR (Bank BJB) | 2023 | Annual financial statements | 0.5 |
| mid-cap-quarterly-filing-id | SMGR (Semen Indonesia) | Q3 2023 | Quarterly (9-month YTD, unaudited) | 0.5 |
| long-form-annual-200p-plus | ASII (Astra International) | 2023 | Annual report (200+ pages) | 0.5 |
| mid-cap-annual-manufacturing-id-digital | INDF (Indofood Sukses Makmur) | 2023 | Annual / financial statements | 0.5 |
| goto-class-small-cap-quarterly-pack | GOTO (GoTo Gojek Tokopedia) | 2023 | Full-year audited financial statements (IDX filing) | 0.5 |
| idx-mining-annual-heavy-tables-id | PTBA (Bukit Asam) | 2023 | Annual report / financial statements | 0.5 |

<!-- PDF acquisition sources (developer obtains BEFORE starting Task 2 — these are NOT downloaded by the executor at runtime) -->

| Manifest id | Required filename in eval/pdfs/ | Primary source URL |
|-------------|--------------------------------|---------------------|
| bbcj-ar-variant-bilingual-large-cap-digital-b | bbcj-ar-variant-bilingual-large-cap-digital-b.pdf | https://www.bankbjb.co.id/files//2024/03/laporan-keuangan-tahun-2023-new.pdf |
| mid-cap-quarterly-filing-id | mid-cap-quarterly-filing-id.pdf | SMGR Q3 2023 filing from sig.id or IDX news/announcement |
| long-form-annual-200p-plus | long-form-annual-200p-plus.pdf | https://r2.astra.co.id/annual-reports/documents/Astra_AR_2023.pdf |
| mid-cap-annual-manufacturing-id-digital | mid-cap-annual-manufacturing-id-digital.pdf | https://www.indofood.com/uploads/statement/INDF_billingual_31_december_2023_released.pdf |
| goto-class-small-cap-quarterly-pack | goto-class-small-cap-quarterly-pack.pdf | https://www.idx.co.id/StaticData/NewsAndAnnouncement/ANNOUNCEMENTSTOCK/From_EREP/202303/20240319163937-42270-0/LK%20GOTO_31%20Des%202023.pdf |
| idx-mining-annual-heavy-tables-id | idx-mining-annual-heavy-tables-id.pdf | https://www.ptba.co.id/id/investor/annual-report (navigate to 2023 report) |

<!-- Approximate audited figures from RESEARCH.md §"Approximate Financial Figures" — STARTING POINTS ONLY, must be verified against actual audited PDFs -->

| Company / Year | Approx revenue (IDR) | Approx net income (IDR) | Approx total assets (IDR) | Special note |
|----------------|----------------------|--------------------------|----------------------------|--------------|
| BJBR 2023 | ~10 T (net interest income + non-interest operating income — same definition as BBCA) | ~1.78 T | ~188.29 T | Bank — use BBCA's revenue semantics (pendapatan operasional, NOT gross interest income) |
| SMGR Q3 2023 | YTD 9-month — less than full-year ~38.65 T | YTD 9-month — less than full-year ~2.17 T | as of 30 Sep 2023 | Column header MUST read "30 September 2023" — quarterly = YTD not full-year |
| ASII 2023 | ~316.56 T | ~33.83 T (net income attr. to owners of parent) | ~570 T | Conglomerate — header MUST read "Konsolidasian", NOT "Entitas Induk". Statements appear deep in 200+ page doc. |
| INDF 2023 | ~123.49 T | ~5.1 T | ~140 T | Filing is bilingual — read Indonesian column. MUST be "Konsolidasian" (group, not parent-only). |
| GOTO 2023 | ~14.78 T net revenue | **negative** ~-90.63 T (net LOSS) | ~54.1 T | valueIDR for net_income MUST be NEGATIVE integer. Revenue = net revenue (NOT gross transaction value/GTV). |
| PTBA 2023 | ~38.5 T | ~6.11 T | ~47 T | Check denomination note — mining filers may use "miliaran" (billions) instead of "jutaan" (millions) |

<!-- From src/lib/eval/schema.ts — fixture schema (already enforced by Zod loadGroundTruth) -->

```typescript
{
  documentId: string,                       // MUST exactly match manifest.json `id`
  fixtureStatus: "ready" | "placeholder",   // MUST become "ready"
  numericExpectations: Array<{
    key: string,                            // exactly: revenue_latest_year, net_income_latest_year,
                                            //          total_assets_latest_year, total_equity_latest_year,
                                            //          operating_cash_flow_latest_year (all 5 if available)
    valueIDR: number,                       // full Rupiah integer (printed × denomination multiplier)
    tolerancePct: number                    // 0.5 for all 6 Wave-2 documents (all digital)
  }>,
  citationExpectations: Array<{
    id: string,                             // exactly: income_statement_citation,
                                            //          balance_sheet_citation,
                                            //          cash_flow_citation
    allowedPages: number[]                  // PDF absolute page index (1-based), 3-page window
  }>,
}
```

<!-- From scripts/eval/run.ts — gate behavior (READ-ONLY for this plan) -->

- All 9 fixtures must be `"ready"` with non-empty arrays → otherwise exit 1.
- All 9 PDFs must exist at `eval/pdfs/<manifest.relativePdf>` → otherwise exit 1.
- After all 9 reach `kind: "ok"`, `aggregateScores()` runs. Defaults: numeric ≥ 90%, citation ≥ 90%. Below either → exit 1.
- `EVAL_PROMPT_VARIANT=broken` causes `extractEvalClaims()` to use `PROMPT_EVAL_BROKEN` from src/lib/eval/prompts.ts, which instructs Gemini to return `valueIDR: 789` for all keys and cite page `424242`. This MUST cause scoring to crater below 90% on at least one of the two thresholds → exit 1 = regression proof.

<!-- From src/lib/eval/prompts.ts —
the prompt variants are already wired; this plan does NOT modify prompts.ts -->

EVAL_MODEL_ID = "gemini-2.5-flash"

<!-- Fixture-update strategy for the 6 placeholder files -->

The 6 placeholder fixture files already exist with this shape:
```json
{
  "documentId": "<existing manifest id>",
  "fixtureStatus": "placeholder",
  "numericExpectations": [],
  "citationExpectations": []
}
```
You will REPLACE the file contents in-place: keep `documentId` identical to the existing value (which already matches the manifest id), flip `fixtureStatus` to `"ready"`, populate the two arrays with 5 numeric + 3 citation entries each (all 6 documents). Use the analog patterns from `eval/fixtures/tlkm-ar-id-only-mid-cap-digital.json` (digital, 0.5 tolerance) — and for BJBR specifically, the analog is `eval/fixtures/bbca-ar-bilingual-large-cap-digital.json` (bank revenue semantics).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Developer obtains 6 audited PDFs and drops them into eval/pdfs/ with exact filenames</name>
  <what-built>
This is a developer manual action — the executor agent does NOT download files at runtime. The six PDFs must be present at the exact filenames declared in `eval/manifest.json` before any extraction can occur.
  </what-built>
  <how-to-verify>
For each of the six PDFs below, navigate to the source URL in a browser, download the file, rename it to the required filename, and place it in the `eval/pdfs/` directory. The required filenames are **non-negotiable** — they are referenced by `eval/manifest.json` `relativePdf` fields and `run.ts` looks up each PDF by that exact path.

1. **BJBR 2023 financial statements**
   - Source: https://www.bankbjb.co.id/files//2024/03/laporan-keuangan-tahun-2023-new.pdf
   - Save as: `eval/pdfs/bbcj-ar-variant-bilingual-large-cap-digital-b.pdf`

2. **SMGR Q3 2023 quarterly filing**
   - Source: SMGR investor relations or IDX news (search "SMGR laporan keuangan 30 September 2023")
   - Save as: `eval/pdfs/mid-cap-quarterly-filing-id.pdf`

3. **ASII 2023 annual report (200+ pages)**
   - Source: https://r2.astra.co.id/annual-reports/documents/Astra_AR_2023.pdf
   - Save as: `eval/pdfs/long-form-annual-200p-plus.pdf`

4. **INDF 2023 bilingual financial statements**
   - Source: https://www.indofood.com/uploads/statement/INDF_billingual_31_december_2023_released.pdf
   - Save as: `eval/pdfs/mid-cap-annual-manufacturing-id-digital.pdf`

5. **GOTO 2023 audited financial statements (IDX-filed)**
   - Source: https://www.idx.co.id/StaticData/NewsAndAnnouncement/ANNOUNCEMENTSTOCK/From_EREP/202303/20240319163937-42270-0/LK%20GOTO_31%20Des%202023.pdf
   - Save as: `eval/pdfs/goto-class-small-cap-quarterly-pack.pdf`

6. **PTBA 2023 annual report**
   - Source: https://www.ptba.co.id/id/investor/annual-report → 2023 report
   - Save as: `eval/pdfs/idx-mining-annual-heavy-tables-id.pdf`

Verify all six are present:
```bash
ls -1 eval/pdfs/ | sort
```
Expected output (9 files total, including the 3 Wave-1 files):
```
bbca-ar-bilingual-large-cap-digital.pdf
bbcj-ar-variant-bilingual-large-cap-digital-b.pdf
goto-class-small-cap-quarterly-pack.pdf
idx-mining-annual-heavy-tables-id.pdf
long-form-annual-200p-plus.pdf
mid-cap-annual-manufacturing-id-digital.pdf
mid-cap-quarterly-filing-id.pdf
small-cap-scanned-annual.pdf
tlkm-ar-id-only-mid-cap-digital.pdf
```

Cross-reference each downloaded PDF with `eval/manifest.json` to confirm filenames match. If a source URL is broken, find an alternative on the company's official IR page (do NOT use third-party mirrors — audited figures must come from official filings).
  </how-to-verify>
  <resume-signal>Type "pdfs-ready" once `ls eval/pdfs/` lists all 9 files with the exact filenames above. If any source URL is unreachable, describe which document and what alternative source was used.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Populate 6 fixture JSONs from audited PDFs (Group A — BJBR, SMGR-Q3, PTBA)</name>
  <files>
    eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json
    eval/fixtures/mid-cap-quarterly-filing-id.json
    eval/fixtures/idx-mining-annual-heavy-tables-id.json
  </files>
  <read_first>
    /Users/hovegant/Documents/GitHub/clarifin/eval/manifest.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/mid-cap-quarterly-filing-id.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/idx-mining-annual-heavy-tables-id.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/bbca-ar-bilingual-large-cap-digital.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/tlkm-ar-id-only-mid-cap-digital.json
    /Users/hovegant/Documents/GitHub/clarifin/.planning/phases/05-indonesian-eval-harness/05-RESEARCH.md
    /Users/hovegant/Documents/GitHub/clarifin/.planning/phases/05-indonesian-eval-harness/05-PATTERNS.md
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/prompts.ts
  </read_first>
  <action>
For each of the three documents below, follow this 5-step procedure:

**Procedure (apply to each of the 3 documents):**

1. **LLM-assisted first pass:** Run the harness with only that document's fixture flipped to a temporary minimal-ready state to capture Gemini's candidate extraction. Actually, simpler approach since we cannot single-out documents through the runner: temporarily run a one-off extraction by inspecting `eval/pdfs/<filename>.pdf` directly through a PDF viewer; the LLM first-pass is optional and only used as a starting hint.

   Pragmatic path: SKIP the LLM first-pass. Go directly to step 2 — human visual extraction from the PDF — which is the authoritative source anyway.

2. **Open the PDF in a viewer that shows absolute page index** ("Page X of Y" indicator). For example: `open eval/pdfs/bbcj-ar-variant-bilingual-large-cap-digital-b.pdf` on macOS opens Preview, which shows absolute page index in the title bar.

3. **Locate the consolidated financial statements:**
   - Income statement: search for "Laporan Laba Rugi Konsolidasian" or "Statements of Profit or Loss" (bilingual)
   - Balance sheet: "Laporan Posisi Keuangan Konsolidasian" or "Statements of Financial Position"
   - Cash flow: "Laporan Arus Kas Konsolidasian" or "Statements of Cash Flows"
   - The table HEADER must say "Konsolidasian" — never "Entitas Induk". For BJBR (a bank), only standalone bank financials may exist; in that case use those.
   - The latest-year column header must be one of: "31 Desember 2023", "December 31, 2023", or "30 September 2023" (for SMGR-Q3 only).

4. **Read the denomination note** (cover page, Note 1, or column header above the figure block):
   - "dalam jutaan Rupiah" / "in millions of Rupiah" → printed × 1,000,000
   - "dalam miliaran Rupiah" / "in billions of Rupiah" → printed × 1,000,000,000
   - "dalam Rupiah penuh" → printed × 1

5. **Transcribe the five standard keys and three citation pages into the fixture JSON.** Overwrite the entire file with the following template, filling in actual integers:

```json
{
  "documentId": "<EXACT manifest id — do not change>",
  "fixtureStatus": "ready",
  "numericExpectations": [
    { "key": "revenue_latest_year",             "valueIDR": <INTEGER>, "tolerancePct": 0.5 },
    { "key": "net_income_latest_year",          "valueIDR": <INTEGER>, "tolerancePct": 0.5 },
    { "key": "total_assets_latest_year",        "valueIDR": <INTEGER>, "tolerancePct": 0.5 },
    { "key": "total_equity_latest_year",        "valueIDR": <INTEGER>, "tolerancePct": 0.5 },
    { "key": "operating_cash_flow_latest_year", "valueIDR": <INTEGER>, "tolerancePct": 0.5 }
  ],
  "citationExpectations": [
    { "id": "income_statement_citation", "allowedPages": [<N>, <N+1>, <N+2>] },
    { "id": "balance_sheet_citation",    "allowedPages": [<M>, <M+1>, <M+2>] },
    { "id": "cash_flow_citation",        "allowedPages": [<P>, <P+1>, <P+2>] }
  ]
}
```
Where N, M, P are PDF absolute page indices (NOT printed footer page numbers) where each statement table begins. The 3-page window covers renderer variance.

**Document-specific rules:**

A. **`bbcj-ar-variant-bilingual-large-cap-digital-b.json` (BJBR 2023):**
   - documentId MUST remain exactly: `bbcj-ar-variant-bilingual-large-cap-digital-b`
   - BJBR is a BANK. Apply BBCA's `revenue_latest_year` semantics: net interest income + non-interest operating income (= pendapatan operasional / total operating income). DO NOT use gross interest income or loan origination volume.
   - Approximate target: ~10 T net interest income (revenue), ~1.78 T net income, ~188 T total assets — verify each from PDF, do NOT commit web estimates.
   - All 5 numeric keys present. If `operating_cash_flow_latest_year` is not on a cash flow statement in this filing, you may omit it (length will be 4); but per PROMPT_EVAL_BASE all 5 are expected and the harness scores against present keys only — keep all 5 if the cash flow statement exists in the PDF.

B. **`mid-cap-quarterly-filing-id.json` (SMGR Q3 2023):**
   - documentId MUST remain exactly: `mid-cap-quarterly-filing-id`
   - This is a QUARTERLY filing. `revenue_latest_year` and `net_income_latest_year` are 9-month YTD figures (1 Jan 2023 – 30 Sep 2023), NOT full-year 2023. The column header MUST say "30 September 2023" — if it says "31 Desember 2023" you have the wrong document.
   - Approximate full-year SMGR 2023 revenue = 38.65 T; Q3 YTD will be lower (perhaps ~25-28 T) — verify exact figure from the PDF.
   - Balance sheet `total_assets_latest_year` and `total_equity_latest_year` are as-of 30 September 2023 (point-in-time, not cumulative).
   - tolerancePct: 0.5 on all entries.

C. **`idx-mining-annual-heavy-tables-id.json` (PTBA 2023):**
   - documentId MUST remain exactly: `idx-mining-annual-heavy-tables-id`
   - Mining filers sometimes state figures in "miliaran Rupiah" (billions, multiplier 10^9), not "jutaan" (millions, 10^6). READ the denomination note carefully. If denomination is miliaran and printed value is `38,500`, valueIDR = 38500 × 1,000,000,000 = 38500000000000.
   - Approximate: revenue ~38.5 T, net income ~6.11 T, total assets ~47 T — verify each from PDF.
   - tolerancePct: 0.5 on all 5 entries.

**Schema sanity (apply to all 3 files):**
- File is valid JSON (parses without error).
- `documentId` field is unchanged from its current value (which matches the manifest id).
- `fixtureStatus` is the exact string `"ready"`.
- `numericExpectations` array length is exactly 5 (or 4 if an item is absent from the PDF — document this case in the SUMMARY).
- `citationExpectations` array length is exactly 3.
- Every `valueIDR` is a positive integer (no banks/miners run net losses in 2023, so no negatives expected here — GOTO is handled in Task 3).
- Every `allowedPages` is an array of exactly 3 positive integers (PDF page indices).
- Every `tolerancePct` is 0.5.
  </action>
  <verify>
    <automated>node -e "['bbcj-ar-variant-bilingual-large-cap-digital-b','mid-cap-quarterly-filing-id','idx-mining-annual-heavy-tables-id'].forEach(id=>{const f=JSON.parse(require('fs').readFileSync('eval/fixtures/'+id+'.json','utf8')); if(f.documentId!==id) throw new Error('documentId mismatch: '+id); if(f.fixtureStatus!=='ready') throw new Error('not ready: '+id); if(f.numericExpectations.length<4||f.numericExpectations.length>5) throw new Error('numeric count: '+id); if(f.citationExpectations.length!==3) throw new Error('citation count: '+id); f.numericExpectations.forEach(n=>{if(typeof n.valueIDR!=='number'||!Number.isInteger(n.valueIDR)||n.valueIDR<=0) throw new Error('bad valueIDR in '+id+': '+JSON.stringify(n)); if(n.tolerancePct!==0.5) throw new Error('tolerance not 0.5: '+id);}); f.citationExpectations.forEach(c=>{if(c.allowedPages.length!==3) throw new Error('allowedPages window != 3: '+id);});}); console.log('GroupA OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 fixture files parses as valid JSON.
    - Each fixture's `documentId` exactly matches its manifest entry (`bbcj-ar-variant-bilingual-large-cap-digital-b`, `mid-cap-quarterly-filing-id`, `idx-mining-annual-heavy-tables-id`).
    - Each fixture has `"fixtureStatus": "ready"`.
    - Each fixture has 4 or 5 entries in `numericExpectations` (omit only if the line item is genuinely absent from the PDF) — for BJBR/SMGR/PTBA the cash flow statement is standard so 5 is the expected count.
    - Each fixture has exactly 3 entries in `citationExpectations` with ids `income_statement_citation`, `balance_sheet_citation`, `cash_flow_citation`.
    - All `valueIDR` values are positive integers (no negatives — none of these companies posted 2023 net losses).
    - All `tolerancePct` values are exactly 0.5.
    - All `allowedPages` arrays have exactly 3 PDF-absolute-page-index integers each.
    - BJBR fixture `revenue_latest_year.valueIDR` is in the range of 5,000,000,000,000 to 20,000,000,000,000 (5–20 T) — reflects net interest income + non-interest operating income, NOT gross interest income (which would be ~14 T but is the wrong line item).
    - SMGR fixture `revenue_latest_year.valueIDR` is LESS than 38,650,000,000,000 (the full-year 2023 figure) — confirms 9-month YTD figure was used, not full-year.
    - PTBA fixture has its denomination correctly multiplied (e.g., if PDF states "miliaran" and prints 38500, fixture stores 38500000000000 not 38500000000).
  </acceptance_criteria>
  <done>
    3 of 6 placeholder fixtures are now audited-`ready` (BJBR, SMGR-Q3, PTBA). Schema valid, denomination correctly applied, bank-revenue semantics preserved for BJBR.
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate 6 fixture JSONs from audited PDFs (Group B — ASII, INDF, GOTO)</name>
  <files>
    eval/fixtures/long-form-annual-200p-plus.json
    eval/fixtures/mid-cap-annual-manufacturing-id-digital.json
    eval/fixtures/goto-class-small-cap-quarterly-pack.json
  </files>
  <read_first>
    /Users/hovegant/Documents/GitHub/clarifin/eval/manifest.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/long-form-annual-200p-plus.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/mid-cap-annual-manufacturing-id-digital.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/goto-class-small-cap-quarterly-pack.json
    /Users/hovegant/Documents/GitHub/clarifin/eval/fixtures/tlkm-ar-id-only-mid-cap-digital.json
    /Users/hovegant/Documents/GitHub/clarifin/.planning/phases/05-indonesian-eval-harness/05-RESEARCH.md
    /Users/hovegant/Documents/GitHub/clarifin/.planning/phases/05-indonesian-eval-harness/05-PATTERNS.md
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/score-run.ts
  </read_first>
  <action>
Apply the same 5-step procedure as Task 2 (open PDF, locate consolidated statements, verify column header is 2023 / 31 Desember 2023, read denomination, transcribe). Document-specific rules:

A. **`long-form-annual-200p-plus.json` (ASII 2023):**
   - documentId MUST remain exactly: `long-form-annual-200p-plus`
   - ASII is a 200+ page conglomerate annual report. The consolidated financial statements will appear DEEP in the document — possibly past PDF page 200. The cover + management discussion + governance sections add 50-150 pages BEFORE the statements.
   - The HEADER must read "Laporan Keuangan Konsolidasian" — NOT "Entitas Induk". The parent-only revenue for ASII is a small fraction of group revenue.
   - `net_income_latest_year` = net income attributable to OWNERS of the parent (not non-controlling interests / minority interest line).
   - Approximate: revenue ~316.56 T (316560000000000); net income ~33.83 T (33830000000000); total assets ~570 T. Verify each from the PDF — these are web aggregator estimates with MEDIUM confidence.
   - `allowedPages`: expect indices in the 200-400 range, not 9-15 like a short filing. Use the PDF viewer's absolute page count.

B. **`mid-cap-annual-manufacturing-id-digital.json` (INDF 2023):**
   - documentId MUST remain exactly: `mid-cap-annual-manufacturing-id-digital`
   - The INDF filing is BILINGUAL (English/Indonesian). Financial tables are in Indonesian.
   - The HEADER must read "Laporan Keuangan Konsolidasian" — INDF has significant subsidiary ICBP, and parent-only revenue would be far smaller than group revenue (~123 T consolidated vs much lower parent-only).
   - Approximate: revenue ~123.49 T, net income ~5.1 T, total assets ~140 T — verify each from PDF.
   - tolerancePct: 0.5 on all 5 entries.

C. **`goto-class-small-cap-quarterly-pack.json` (GOTO 2023 full-year):**
   - documentId MUST remain exactly: `goto-class-small-cap-quarterly-pack`
   - GOTO posted a NET LOSS in 2023. `net_income_latest_year.valueIDR` MUST be a NEGATIVE integer.
   - Approximate: revenue ~14.78 T net revenue (positive); net loss ~-90.63 T → `valueIDR: -90630000000000`. Verify exact net loss figure from the PDF.
   - Revenue MUST be the net revenue line (revenues net of incentives/promotions), NOT Gross Transaction Value (GTV) which is far larger.
   - The `withinTolerance()` function in `src/lib/eval/score-run.ts` uses `Math.abs(expected)` in the denominator — negative `valueIDR` is mathematically supported. Confirm by reading the function before committing the negative value.
   - tolerancePct: 0.5 on all 5 entries.
   - All other keys (revenue, total_assets, total_equity, operating_cash_flow) follow normal positive-integer rules. Note `total_equity_latest_year` may be unusually small or even negative if accumulated losses exceed paid-in capital — verify from the balance sheet, do not assume positive.

**Schema sanity (apply to all 3 files):**
- File is valid JSON.
- `documentId` unchanged from current value (matches manifest).
- `fixtureStatus`: `"ready"`.
- `numericExpectations`: 4 or 5 entries (5 is the standard).
- `citationExpectations`: exactly 3 entries with ids `income_statement_citation`, `balance_sheet_citation`, `cash_flow_citation`.
- `tolerancePct`: 0.5 everywhere.
- For ASII/INDF: every `valueIDR` is a positive integer (large profitable conglomerates).
- For GOTO: `net_income_latest_year.valueIDR` is a NEGATIVE integer; other keys are positive (except potentially total_equity if balance sheet shows accumulated-deficit-driven negative equity — extremely rare for GOTO but verify).
- `allowedPages`: 3-page windows; for ASII expect indices > 200.
  </action>
  <verify>
    <automated>node -e "const ids=['long-form-annual-200p-plus','mid-cap-annual-manufacturing-id-digital','goto-class-small-cap-quarterly-pack']; ids.forEach(id=>{const f=JSON.parse(require('fs').readFileSync('eval/fixtures/'+id+'.json','utf8')); if(f.documentId!==id) throw new Error('documentId mismatch: '+id); if(f.fixtureStatus!=='ready') throw new Error('not ready: '+id); if(f.numericExpectations.length<4||f.numericExpectations.length>5) throw new Error('numeric count: '+id); if(f.citationExpectations.length!==3) throw new Error('citation count: '+id); f.numericExpectations.forEach(n=>{if(typeof n.valueIDR!=='number'||!Number.isInteger(n.valueIDR)) throw new Error('bad valueIDR: '+id+' '+JSON.stringify(n)); if(n.tolerancePct!==0.5) throw new Error('tolerance not 0.5: '+id);}); f.citationExpectations.forEach(c=>{if(c.allowedPages.length!==3) throw new Error('allowedPages window != 3: '+id);});}); const goto=JSON.parse(require('fs').readFileSync('eval/fixtures/goto-class-small-cap-quarterly-pack.json','utf8')); const ni=goto.numericExpectations.find(n=>n.key==='net_income_latest_year'); if(!ni||ni.valueIDR>=0) throw new Error('GOTO net_income_latest_year must be NEGATIVE integer'); console.log('GroupB OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 fixture files parses as valid JSON.
    - Each fixture's `documentId` matches its manifest entry (`long-form-annual-200p-plus`, `mid-cap-annual-manufacturing-id-digital`, `goto-class-small-cap-quarterly-pack`).
    - Each has `"fixtureStatus": "ready"`, 4–5 numeric entries, exactly 3 citation entries.
    - All `tolerancePct` values are 0.5.
    - ASII and INDF `valueIDR` values are all positive integers.
    - GOTO `net_income_latest_year.valueIDR` is a NEGATIVE integer (net loss).
    - ASII `allowedPages` indices are > 100 (statements appear deep in the 200+ page document).
    - GOTO `revenue_latest_year.valueIDR` is in the range 10,000,000,000,000 to 20,000,000,000,000 (10–20 T net revenue — NOT GTV which would be > 100 T).
  </acceptance_criteria>
  <done>
    All 6 Wave-2 placeholder fixtures are now audited-ready. Schema valid, GOTO negative net income stored correctly, ASII deep-page citation indices recorded.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run full 9-document corpus through pnpm eval — gate must exit 0</name>
  <files>
    eval/fixtures/bbcj-ar-variant-bilingual-large-cap-digital-b.json
    eval/fixtures/mid-cap-quarterly-filing-id.json
    eval/fixtures/long-form-annual-200p-plus.json
    eval/fixtures/mid-cap-annual-manufacturing-id-digital.json
    eval/fixtures/goto-class-small-cap-quarterly-pack.json
    eval/fixtures/idx-mining-annual-heavy-tables-id.json
  </files>
  <read_first>
    /Users/hovegant/Documents/GitHub/clarifin/scripts/eval/run.ts
    /Users/hovegant/Documents/GitHub/clarifin/eval/manifest.json
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/score-run.ts
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/prompts.ts
  </read_first>
  <action>
Step 1 — Confirm all 9 PDFs and all 9 fixtures are ready:
```bash
ls eval/pdfs/ | wc -l   # expect 9
node -e "const m=JSON.parse(require('fs').readFileSync('eval/manifest.json','utf8')); m.documents.forEach(d=>{const f=JSON.parse(require('fs').readFileSync('eval/fixtures/'+d.id+'.json','utf8')); if(f.fixtureStatus!=='ready') throw new Error('not ready: '+d.id);}); console.log('All 9 ready');"
```

Step 2 — Run the full corpus:
```bash
pnpm eval 2>&1 | tee /tmp/clarifin-wave2-eval.log
echo "Exit code: $?"
```
Expected: exit 0. If any document exits `✗`, check the per-document failure lines in the log and fix the corresponding fixture (return to Task 2 or Task 3 for that document).

Step 3 — Confirm aggregate thresholds met:
```bash
grep -E "Aggregate|Overall|numeric|citation" /tmp/clarifin-wave2-eval.log | head -10
```
Both overall numeric% and overall citation% must be ≥ 90.0%.

Step 4 — If any fixture fails at < 90%, apply the same PDF cross-check procedure from Plan 05-01 Task 2 to the failing document (open PDF, verify denomination, re-check allowedPages, correct valueIDR). Re-run after each fix. Do NOT widen tolerancePct to mask a wrong audited value.
  </action>
  <verify>
    <automated>pnpm eval; echo "eval exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm eval` exits 0 (all 9 documents reach `kind: "ok"` and both aggregate percentages ≥ 90%).
    - `/tmp/clarifin-wave2-eval.log` contains 9 lines starting with `✓` (no `✗` rows).
    - `grep -c '^✓' /tmp/clarifin-wave2-eval.log` returns 9.
    - The aggregate numeric% printed in the log is ≥ 90.0%.
    - The aggregate citation% printed in the log is ≥ 90.0%.
  </acceptance_criteria>
  <done>
    Full 9-document corpus passes the eval gate. `pnpm eval` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 5: Broken-prompt regression proof — gate must exit non-zero with EVAL_PROMPT_VARIANT=broken</name>
  <files></files>
  <read_first>
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/prompts.ts
    /Users/hovegant/Documents/GitHub/clarifin/src/lib/eval/gemini-eval-extract.ts
    /Users/hovegant/Documents/GitHub/clarifin/scripts/eval/run.ts
  </read_first>
  <action>
Step 1 — Confirm how the broken-prompt variant is activated. Read `src/lib/eval/prompts.ts` and `src/lib/eval/gemini-eval-extract.ts` to find the exact env var or param name. The AI-SPEC documents it as `EVAL_PROMPT_VARIANT=broken` but verify the actual implementation before running.

Step 2 — Run the broken-prompt variant:
```bash
EVAL_PROMPT_VARIANT=broken pnpm eval 2>&1 | tee /tmp/clarifin-wave2-broken.log
echo "Broken exit code: $?"
```
Expected: exit non-zero (1). The broken prompt (`PROMPT_EVAL_BROKEN` in prompts.ts) instructs Gemini to return `valueIDR: 789` for all keys and cite page `424242` — this will produce near-0% numeric accuracy and 0% citation accuracy, both below the 90% threshold → exit 1.

Step 3 — Confirm:
- Exit code is 1 (not 0).
- The log shows both aggregate numeric% and citation% below 90%.
- At least one `✗` row appears (some documents may still reach `kind: "ok"` but score below threshold).

If the exit code is 0 (broken prompt passed the gate), something is wrong with the fault-injection wiring. DO NOT proceed — stop and report: "GATE IS DECORATIVE: broken-prompt variant passed. Investigation required."
  </action>
  <verify>
    <automated>EVAL_PROMPT_VARIANT=broken pnpm eval; test $? -ne 0 && echo "GATE LIVE: broken prompt correctly rejected" || echo "GATE BROKEN: broken prompt passed incorrectly"</automated>
  </verify>
  <acceptance_criteria>
    - `EVAL_PROMPT_VARIANT=broken pnpm eval` exits non-zero (exit code ≠ 0).
    - `/tmp/clarifin-wave2-broken.log` contains no aggregate percentages ≥ 90% for both numeric and citation (the broken prompt specifically targets these dimensions).
    - The word "DECORATIVE" does NOT appear in the executor's output — the gate is live.
  </acceptance_criteria>
  <done>
    Broken-prompt regression proof captured. The eval gate is non-decorative: misconfiguring the Gemini extraction prompt causes the harness to score below threshold and report specific failures.
  </done>
</task>

<task type="auto">
  <name>Task 6: Append Wave 2 sign-off block to eval/README.md</name>
  <files>eval/README.md</files>
  <read_first>
    /Users/hovegant/Documents/GitHub/clarifin/eval/README.md
    /tmp/clarifin-wave2-eval.log
    /tmp/clarifin-wave2-broken.log
  </read_first>
  <action>
Append the following section to `eval/README.md` below the existing Wave 1 baseline section:

```markdown
## Wave 2 gate live (locked YYYY-MM-DD)

All 9 eval documents pass `pnpm eval` at ≥90% numeric AND ≥90% citation accuracy.
Broken-prompt regression proof: `EVAL_PROMPT_VARIANT=broken pnpm eval` exits 1 with both percentages below threshold — the gate is non-decorative.

### Full-corpus results

| Document | Numeric | Citation |
|----------|---------|----------|
| bbca-ar-bilingual-large-cap-digital | <from log> | <from log> |
| tlkm-ar-id-only-mid-cap-digital | <from log> | <from log> |
| small-cap-scanned-annual | <from log> | <from log> |
| bbcj-ar-variant-bilingual-large-cap-digital-b | <from log> | <from log> |
| mid-cap-quarterly-filing-id | <from log> | <from log> |
| long-form-annual-200p-plus | <from log> | <from log> |
| mid-cap-annual-manufacturing-id-digital | <from log> | <from log> |
| goto-class-small-cap-quarterly-pack | <from log> | <from log> |
| idx-mining-annual-heavy-tables-id | <from log> | <from log> |

**Overall aggregate:** numeric <X.X>%, citation <Y.Y>%

### Broken-prompt regression proof

Running `EVAL_PROMPT_VARIANT=broken pnpm eval` exits 1.
Broken-prompt numeric: <from /tmp/clarifin-wave2-broken.log>%, citation: <%> — both below 90% threshold.

### Phase 6 gate status

`pnpm eval` exits 0. Phase 6 (AI Explanation Generation) may proceed.
Any future AI explanation changes must not cause `pnpm eval` to exit non-zero.
```

Replace YYYY-MM-DD with today's date (`date +%Y-%m-%d`). Fill in all `<from log>` placeholders from the actual log files. Do NOT leave any `<...>` placeholders in the committed file.
  </action>
  <verify>
    <automated>grep -q "Wave 2 gate live" eval/README.md && grep -q "Phase 6 gate status" eval/README.md && grep -q "EVAL_PROMPT_VARIANT=broken" eval/README.md && ! grep -q "<from log>" eval/README.md && echo "README OK" || echo "README INCOMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `eval/README.md` contains `## Wave 2 gate live` heading.
    - `eval/README.md` contains `Phase 6 gate status` section.
    - `eval/README.md` contains `EVAL_PROMPT_VARIANT=broken` reference.
    - No `<from log>` or `<X.X>` placeholders remain (`grep -c '<from log>\|<X\.X>\|<Y\.Y>' eval/README.md` returns 0).
    - All 9 document ids appear in the results table.
  </acceptance_criteria>
  <done>
    Wave 2 sign-off locked in eval/README.md. Phase 6 gate is documented as live and non-decorative.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Human sign-off — full 9-doc gate live, Phase 6 cleared to start</name>
  <what-built>
All 9 IDX eval documents have audited-ready fixtures. `pnpm eval` exits 0 with ≥90% aggregate numeric and citation accuracy. The broken-prompt gate exits 1. Phase 6 (AI Explanation Generation) is cleared to start.
  </what-built>
  <how-to-verify>
1. Full-corpus green run:
   ```bash
   pnpm eval
   echo "Exit: $?"
   ```
   Confirm exit 0 and 9 `✓` rows.

2. Broken-prompt gate:
   ```bash
   EVAL_PROMPT_VARIANT=broken pnpm eval
   echo "Exit: $?"
   ```
   Confirm exit 1 (non-zero).

3. README sign-off:
   ```bash
   grep -A 30 "## Wave 2 gate live" eval/README.md
   ```
   Confirm no placeholder text remains.
  </how-to-verify>
  <resume-signal>Type "gate-live" once all three checks pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → Gemini Files API | PDF bytes uploaded over HTTPS to Google; auto-deleted after 48h TTL; contains only PUBLIC IDX filings (no PII). |
| Fixture JSON files → harness runtime | Local file system read; data is human-curated ground truth; influences gate outcome. |
| `pnpm eval` exit code → Phase 6 gate | Exit 0/1 gates Phase 6 work. Broken-prompt proof validates the gate is non-decorative. |
| PDF source URLs → developer workstation | Developer downloads from official company IR / IDX pages; third-party mirrors are prohibited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Wrong audited value committed (wrong denomination, wrong table) | mitigate | 5-step procedure in Tasks 2+3 requires human PDF visual verification: confirm "Konsolidasian" header, latest-year column date, denomination note. Acceptance criteria include sanity-range checks (e.g., BJBR revenue 5–20 T, SMGR YTD < full-year). |
| T-05-02-02 | Tampering | tolerancePct widened to mask a wrong figure | mitigate | Acceptance criteria require exactly 0.5 for all Wave-2 fixtures. `<action>` blocks explicitly prohibit widening tolerance — only correct the `valueIDR` or `allowedPages`. |
| T-05-02-03 | Spoofing | GOTO net_income_latest_year committed as positive (masks net loss) | mitigate | Task 3 verify script requires `ni.valueIDR < 0`; acceptance criteria explicitly check for NEGATIVE integer. |
| T-05-02-04 | Elevation of Privilege | Gate widened to accommodate wrong fixtures (threshold lowered) | mitigate | `run.ts` thresholds are READ-ONLY for this plan — `files_modified` frontmatter lists only fixture JSON + README. Task 5 acceptance criteria confirm exit code ≠ 0 for broken prompt, proving threshold was not lowered. |
| T-05-02-05 | Denial of Service | Gemini quota exhaustion (250 RPD free tier) | accept | Wave 2 requires ~3 eval runs × 9 calls = 27 calls. Plus broken-prompt run = 9 more = 36 total. Well within 250 RPD. |
| T-05-02-06 | Information Disclosure | GEMINI_API_KEY in shell history or log files | mitigate | Key read from `.env.local` (gitignored); logs go to `/tmp/`; fixture JSONs and README never contain the key. |
| T-05-02-07 | Repudiation | "The gate passed but it's wrong" — no audit trail | mitigate | Task 6 records per-document scores and the broken-prompt exit code in `eval/README.md`; Task 7 human checkpoint spot-checks both runs. |
</threat_model>

<verification>
1. **All 9 fixtures ready:** `node -e "const m=JSON.parse(require('fs').readFileSync('eval/manifest.json','utf8')); m.documents.forEach(d=>{const f=JSON.parse(require('fs').readFileSync('eval/fixtures/'+d.id+'.json','utf8')); if(f.fixtureStatus!=='ready') throw new Error(d.id);}); console.log('all ready');"` exits 0.
2. **All 9 PDFs present:** `ls eval/pdfs/ | wc -l` returns 9.
3. **Gate green:** `pnpm eval` exits 0 with 9 `✓` rows and aggregate numeric% + citation% both ≥ 90%.
4. **Gate non-decorative:** `EVAL_PROMPT_VARIANT=broken pnpm eval` exits 1 with sub-threshold scores.
5. **README locked:** `grep -q "Wave 2 gate live" eval/README.md && grep -q "Phase 6 gate status" eval/README.md` returns true.
6. **No harness code modified:** `git diff scripts/eval/run.ts src/lib/eval/` shows empty diff.
</verification>

<success_criteria>
- All `must_haves.truths` are observably true via the verification commands above.
- All 9 `must_haves.artifacts` exist with `"fixtureStatus": "ready"` and valid JSON schemas.
- `must_haves.key_links` are all exercised: fixtures → run.ts → extractEvalClaims → Gemini → scoreDocument → aggregateScores → exit 0; and broken prompt → exit 1.
- Phase 6 planning and execution may now proceed — the eval harness is the live gate.
</success_criteria>

<output>
After completion, create `.planning/phases/05-indonesian-eval-harness/05-02-SUMMARY.md` documenting:
- Per-document live scores from the final `pnpm eval` run
- Broken-prompt regression proof: exact exit code and aggregate scores
- Any fixture corrections made during execution (which fields, why)
- Confirmation that no harness code under `src/lib/eval/` or `scripts/eval/` was modified
- Phase 6 gate status: "LIVE — pnpm eval exits 0; broken prompt exits 1"
</output>