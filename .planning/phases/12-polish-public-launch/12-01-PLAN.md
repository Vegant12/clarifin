---
plan_id: 12-01
slug: inline-disclaimer-labels
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/doc/explanation-panel.tsx
  - src/components/doc/explanation-panel.test.tsx
  - src/components/chat/chat-interface.tsx
  - src/components/chat/__tests__/chat-interface-disclaimer.test.tsx
autonomous: true
requirements:
  - DISCLAIM-01
estimated_complexity: low
has_visual_checkpoint: false

must_haves:
  truths:
    - "The explanation article element contains the text 'AI analysis · not financial advice'"
    - "The chat input form area contains the text 'AI analysis · not financial advice'"
    - "The score card continues to show 'AI Assessment · not financial advice' adjacent to the score number (regression guard)"
  artifacts:
    - path: "src/components/doc/explanation-panel.tsx"
      provides: "Inline disclaimer paragraph appended inside the <article> element"
      contains: "AI analysis · not financial advice"
    - path: "src/components/chat/chat-interface.tsx"
      provides: "Inline disclaimer paragraph beneath the chat <form> element"
      contains: "AI analysis · not financial advice"
    - path: "src/components/doc/explanation-panel.test.tsx"
      provides: "Vitest assertion that disclaimer renders in ExplanationPanel"
    - path: "src/components/chat/__tests__/chat-interface-disclaimer.test.tsx"
      provides: "Vitest assertion that disclaimer renders in ChatInterface"
  key_links:
    - from: "src/components/doc/explanation-panel.tsx"
      to: "DISCLAIM-01"
      via: "Disclaimer <p> rendered inside <article>"
      pattern: "AI analysis.*not financial advice"
    - from: "src/components/chat/chat-interface.tsx"
      to: "DISCLAIM-01"
      via: "Disclaimer <p> rendered after <form>"
      pattern: "AI analysis.*not financial advice"
---

<objective>
Add "AI analysis · not financial advice" inline labels to the explanation section and chat input area, closing the two open gaps in DISCLAIM-01. The score card already has this label at line 37–39 of score-card.tsx — this plan adds it to the two remaining locations.

Purpose: DISCLAIM-01 requires the disclaimer to be adjacent to the score, explanation, and chat — not only in the page footer. The footer disclaimer already exists on page.tsx; score-card.tsx already covers the score. This plan closes the explanation and chat gaps.

Output: Two modified component files, two test files. No new dependencies. No migration. No env changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key patterns extracted from codebase for executor reference. -->

From src/components/doc/score-card.tsx lines 37-39 (existing disclaimer — copy this style):
```tsx
<span className="text-muted-foreground text-sm">
  AI Assessment · not financial advice
</span>
```

From src/components/chat/chat-message.tsx lines 72-74 (existing per-message disclaimer):
```tsx
<p className="text-xs text-muted-foreground px-1">
  This is not investment advice.
</p>
```

From src/app/page.tsx lines 8, 43-45 (existing hero disclaimer — copy text pattern):
```tsx
const HERO_DISCLAIMER = "AI analysis · not financial advice.";
<p data-testid="hero-disclaimer" className="text-muted-foreground text-sm italic">
  {HERO_DISCLAIMER}
</p>
```

From src/components/doc/explanation-panel.tsx lines 107-178 (structure executor must modify):
- The <article> element (line 108) wraps all content.
- The SECTION_ORDER.map() block ends at line 176.
- Line 177 closes </article>. The disclaimer paragraph goes BEFORE line 177 (as the last child of <article>).

From src/components/chat/chat-interface.tsx lines 116-168 (structure executor must modify):
- <form> element begins at line 116 and ends at line 158.
- Lines 159-167: conditional "Setting up your chat session…" paragraph.
- Line 168: closes the outer <div>.
- The disclaimer goes AFTER the </form> closing tag (line 158), before the session-ready status paragraph (line 159).
- The outer div (line 91) already uses className="flex flex-col gap-4 h-full".

From src/components/doc/explanation-panel.test.tsx (test file to modify):
- Uses @testing-library/react render + screen.
- renderPanel() helper at line 34 renders ExplanationPanel with fixture data.
- Existing tests cover stock/chart slots; append a new describe block.

data-testid convention (from src/app/page.tsx lines 40, 44):
- Add data-testid="explanation-disclaimer" on the ExplanationPanel paragraph.
- Add data-testid="chat-disclaimer" on the ChatInterface paragraph.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add disclaimer to ExplanationPanel and write test</name>
  <files>
    src/components/doc/explanation-panel.tsx
    src/components/doc/explanation-panel.test.tsx
  </files>
  <behavior>
    - Test: screen.getByTestId("explanation-disclaimer") is in the document after renderPanel()
    - Test: text content equals "AI analysis · not financial advice. Verify all figures against the source PDF."
  </behavior>
  <action>
    Write the test first in explanation-panel.test.tsx: add a new describe block "ExplanationPanel — DISCLAIM-01 inline disclaimer" with one it() that calls renderPanel() and asserts screen.getByTestId("explanation-disclaimer") is in the document and has the expected text. Run `pnpm test -- explanation-panel` — it must fail RED.

    Then modify src/components/doc/explanation-panel.tsx:

    Inside the return statement, at the very end of the <article> element — after the SECTION_ORDER.map() closing `})}` and before the </article> closing tag — add this paragraph as the last child of <article>:

    ```tsx
    <p
      data-testid="explanation-disclaimer"
      className="text-xs text-muted-foreground px-6 pb-4"
    >
      AI analysis · not financial advice. Verify all figures against the source PDF.
    </p>
    ```

    This follows the exact pattern from src/components/chat/chat-message.tsx lines 72-74 (same class shape) and the PATTERNS.md pattern for ExplanationPanel (section 1, last paragraph of the article).

    Do NOT add it inside a section element. It must be a direct child of <article>, placed after the SECTION_ORDER.map() block and before </article>. Do NOT modify SECTION_LABELS, SECTION_ORDER, or any existing section rendering logic.

    Run tests again — must go GREEN.
  </action>
  <verify>
    <automated>cd /Users/hovegant/Documents/GitHub/clarifin && pnpm test -- explanation-panel --run 2>&1 | tail -20</automated>
  </verify>
  <done>All explanation-panel tests pass including the new DISCLAIM-01 disclaimer test. The paragraph with data-testid="explanation-disclaimer" and text "AI analysis · not financial advice. Verify all figures against the source PDF." is rendered as the last child of the &lt;article&gt; element in ExplanationPanel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add disclaimer to ChatInterface and write test</name>
  <files>
    src/components/chat/chat-interface.tsx
    src/components/chat/__tests__/chat-interface-disclaimer.test.tsx
  </files>
  <behavior>
    - Test: renders a paragraph with data-testid="chat-disclaimer" in the ChatInterface output
    - Test: text content equals "AI analysis · not financial advice."
    - Test: the paragraph is outside the chat message log (not inside the role="log" div)
  </behavior>
  <action>
    Create src/components/chat/__tests__/chat-interface-disclaimer.test.tsx as a new file.

    The test must mock `ai/react` because useChat is a hook requiring a server (mock it to return empty messages and no-op handlers). Use vi.mock("ai/react", ...) to return a stub. The test renders ChatInterface with props: documentId="doc-1", sessionId="session-1", initialMessages=[], starterQuestions=[], onGoToPage={() => {}}. Assert screen.getByTestId("chat-disclaimer") is in the document.

    Use this test file header (/** @vitest-environment jsdom */) matching the project's existing test pattern from src/components/chat/__tests__/starter-questions.test.tsx line 1.

    Run `pnpm test -- chat-interface-disclaimer` — must fail RED.

    Then modify src/components/chat/chat-interface.tsx:

    After the closing </form> tag (currently at line 158), and BEFORE the conditional "Setting up your chat session…" paragraph (currently at line 159), add:

    ```tsx
    <p
      data-testid="chat-disclaimer"
      className="text-xs text-muted-foreground"
    >
      AI analysis · not financial advice.
    </p>
    ```

    This disclaimer is always rendered (not conditional). It sits in the outer flex-col div beneath the form, giving it visual proximity to the chat input area as required by DISCLAIM-01. The className follows the `text-xs text-muted-foreground` pattern from chat-message.tsx.

    Do NOT place the disclaimer inside the <form> element. Do NOT modify existing state logic, the useChat call, the textarea, the submit button, or the error/session-ready conditional paragraphs.

    Run tests again — must go GREEN.
  </action>
  <verify>
    <automated>cd /Users/hovegant/Documents/GitHub/clarifin && pnpm test -- chat-interface-disclaimer --run 2>&1 | tail -20</automated>
  </verify>
  <done>New test file passes. The paragraph with data-testid="chat-disclaimer" and text "AI analysis · not financial advice." is rendered in ChatInterface outside the role="log" div, after the form element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component render | Static text rendered to DOM — no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | Disclaimer text in components | accept | Static compile-time text; no runtime source. Cannot be tampered by users. |
</threat_model>

<verification>
Run the full test suite to confirm no regressions:

```bash
cd /Users/hovegant/Documents/GitHub/clarifin && pnpm test --run 2>&1 | tail -30
```

Manual spot-check (optional, not a blocking gate for this autonomous plan):
- Open the document reader UI and verify the disclaimer appears beneath the Key Risks section.
- Open the chat panel and verify the disclaimer appears below the chat input send button.
</verification>

<success_criteria>
1. `pnpm test --run` passes all tests including the two new disclaimer tests.
2. `src/components/doc/explanation-panel.tsx` contains a `<p data-testid="explanation-disclaimer">` element as the last child of `<article>` with text "AI analysis · not financial advice. Verify all figures against the source PDF."
3. `src/components/chat/chat-interface.tsx` contains a `<p data-testid="chat-disclaimer">` element after `</form>` with text "AI analysis · not financial advice."
4. No existing tests regress.
5. TypeScript compiles without errors: `pnpm tsc --noEmit`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish-public-launch/12-01-SUMMARY.md` with:
- Files modified
- Disclaimer text used in each location
- Test names and what they assert
- Any deviations from the plan
</output>
