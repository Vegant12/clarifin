---
phase: 13-t1-data-and-indicators
plan: 07
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06]
files_modified:
  - src/app/ta/[ticker]/page.tsx
  - src/app/ta/page.tsx
  - src/app/ta/ta-chart-shell.tsx
  - src/lib/ta/jobs/refresh-ohlcv.ts
  - src/app/api/internal/dispatch/route.ts
  - vercel.json
  - next.config.ts
  - src/app/api/ta/onnx-smoke/route.ts
  - public/ta/dummy-model.onnx
  - tests/ta/sparse-data.test.ts
  - tests/ta/dispatcher-auth.test.ts
  - package.json
autonomous: false
requirements: [TA-CHART-07, TA-CHART-08, TA-INFRA-02, TA-INFRA-04]
must_haves:
  truths:
    - "Navigating to /ta/{TICKER} renders the full chart surface from cached analysis; lowercase 301s to uppercase"
    - "Invalid tickers, sparse data (<30 candles), in-flight loads, and <640px viewports all show the correct state without NaN or exceptions"
    - "Exactly 2 Vercel crons exist (dispatcher daily + weekly); dispatcher invokes jobs via direct function imports and runTaRefreshOhlcv appends the latest bar"
    - "Dispatcher auth returns 200 with the correct secret and 401 without (R1 implicitly closed)"
    - "ONNX hello-world cold INIT_DURATION is measured on a preview deploy and recorded in VERIFICATION.md"
  artifacts:
    - path: "src/app/ta/[ticker]/page.tsx"
      provides: "RSC page gating sparse/error/mobile and mounting the chart shell"
      contains: "redirect"
    - path: "src/app/api/internal/dispatch/route.ts"
      provides: "single cron dispatcher with direct function imports"
      contains: "runTaRefreshOhlcv"
    - path: "vercel.json"
      provides: "exactly 2 dispatcher crons (daily + weekly)"
      contains: "/api/internal/dispatch"
  key_links:
    - from: "src/app/ta/[ticker]/page.tsx"
      to: "/api/ta/analysis/[ticker]"
      via: "server-side fetch of AnalysisPayload"
      pattern: "api/ta/analysis"
    - from: "src/app/api/internal/dispatch/route.ts"
      to: "src/lib/ta/jobs/refresh-ohlcv.ts"
      via: "import runTaRefreshOhlcv (direct function, not self-fetch)"
      pattern: "runTaRefreshOhlcv"
    - from: "src/app/api/internal/dispatch/route.ts"
      to: "src/lib/internal-auth.ts"
      via: "resolveCandidate + timingSafeStringEq"
      pattern: "resolveCandidate"
---

<objective>
Wire everything into the live surface and consolidate the cron infrastructure: the RSC page at `/ta/[ticker]` (gates sparse/error/mobile, mounts the Plan 05 chart via a client shell that owns range/overlay state), the `/ta` landing page (TickerSearch), the `runTaRefreshOhlcv` job, the single `/api/internal/dispatch` cron that replaces the two v1.0 crons via direct function imports, and the TA-INFRA-04 ONNX hello-world smoke. Green the final two tests (sparse-data, dispatcher-auth).

Purpose: This is the integration wave. The dispatcher cutover implicitly closes v1.0 blocker R1 (one agreed auth path). The ONNX smoke de-risks Phase 15's architecture before it commits.
Output: Two pages + chart shell, the refresh job, the dispatcher, vercel.json cutover, ONNX smoke route + dummy model, two green tests, and the [BLOCKING] measurement recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-t1-data-and-indicators/13-RESEARCH.md
@.planning/phases/13-t1-data-and-indicators/13-PATTERNS.md
@.planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md
@.planning/phases/13-t1-data-and-indicators/13-CONTEXT.md
@.planning/phases/13-t1-data-and-indicators/13-VALIDATION.md

<interfaces>
From Plan 04: GET /api/ta/analysis/[ticker] returns { data: AnalysisPayload | null, error }. AnalysisPayload = { ticker, name_en, last_updated, ohlcv[], indicators, snapshot, candle_count, sparse }. src/lib/ta/ticker-route.ts exports normalizeTickerParam(raw).
From Plan 05: CandlestickChart, RangeSelector, OverlayToggles, IndicatorSnapshotStrip; chart-types RangeKey/OverlayKey/RANGE_TO_DAYS.
From Plan 06: SiteHeader (mounted), TickerSearch, TAPageSkeleton, TAErrorCard, SparseDataCard, MobileInfoCard.
From Plan 01: src/lib/internal-auth.ts (timingSafeStringEq, resolveCandidate); src/lib/ta/fetch-ohlcv.ts + upsert-ohlcv.ts.
v1.0 jobs (signature { docId }): runParseBatch, runEmbedBatch, runAnalyzeBatch from @/lib/ingest/*.
env.INTERNAL_PARSE_SECRET exists. RSC analog: src/app/doc/[documentId]/page.tsx. Base URL: env.CLARIFIN_APP_URL ?? VERCEL_URL ?? localhost.

CRITICAL (PATTERNS.md + parse-batch lines 13-22): dispatcher MUST use direct function imports, NEVER fetch() self-call (508 INFINITE_LOOP_DETECTED). Vercel cron GET cannot send headers → ?secret= query param path required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RSC page + landing page + client chart shell (gates + sparse test)</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <read_first>
    - src/app/doc/[documentId]/page.tsx (RSC fetch + gate-states pattern to mirror)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (ta page section lines 543-609)
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (page layout lines 132-190)
    - src/lib/ta/ticker-route.ts (Plan 04 normalizeTickerParam)
    - src/app/ta/[ticker] consumers: Plan 05 + Plan 06 component exports (props)
  </read_first>
  <action>
    1. Create `src/app/ta/ta-chart-shell.tsx` (`"use client"`): owns range + overlay state and composes Plan 05 components. Props: the full AnalysisPayload + ticker + name_en + last_updated. State: `range` (default "1Y"), `overlays` (default `{ BB:false, EMA20:false, EMA50:true, EMA200:true }`). Slice `ohlcv`/`indicators` to the last `RANGE_TO_DAYS[range]` bars and pass to `<CandlestickChart>` (which re-renders on change). Render `<RangeSelector>`, `<OverlayToggles>`, `<CandlestickChart>`, `<IndicatorSnapshotStrip snapshot={snapshot}>`. Overlay toggles flip series visibility via the refs Plan 05 exposes (pass `overlays` down to CandlestickChart which applies `series.applyOptions({visible})`). Page heading "{TICKER} — {name_en}" text-xl semibold + "IDX · Last updated: {last_updated}" text-xs muted.
    2. Create `src/app/ta/[ticker]/page.tsx` (RSC, no "use client"):
       - Await `params.ticker`. Use `normalizeTickerParam`: if it returns a `redirectTo`, call `redirect(redirectTo)` (TA-TICKER-02 lowercase→uppercase 301). If `!valid`, render `<TAErrorCard ticker={raw} />`.
       - D-05 mobile gate: render a `<div className="block sm:hidden"><MobileInfoCard /></div>` and wrap the chart surface in `<div className="hidden sm:block">`. (CSS-based gate is simplest and SSR-safe; avoids UA sniffing.)
       - Fetch analysis: `fetch(`${baseUrl}/api/ta/analysis/${ticker}`, { next: { revalidate: 3600 } })`. On non-200 or `data: null` with a "not found" error → `<TAErrorCard ticker={ticker} />`. On fetch/transport error → `<TAErrorCard variant="fetch-error" ticker={ticker} />`.
       - Gate sparse: if `data.sparse === true || data.candle_count < 30` → `<SparseDataCard ticker={ticker} />` (no chart, no indicators — TA-CHART-08, no NaN).
       - Else render the desktop wrapper containing `<TickerSearch>` (top, max-w-sm) + `<TaChartShell {...data} />`.
       - `export const metadata` or `generateMetadata` → title "{TICKER} — TA Analysis · Clarifin".
    3. Create `src/app/ta/page.tsx` (RSC) — the `/ta` landing: page container with heading + centered `<TickerSearch />` (and the SiteHeader is already global from Plan 06).
    4. Create `tests/ta/sparse-data.test.ts` (TA-CHART-08): unit-test a pure gate helper. Extract `shouldRenderSparse(payload: { candle_count: number; sparse: boolean }): boolean` into `src/lib/ta/ticker-route.ts` (or a sibling) and assert: candle_count 10 → true; candle_count 250, sparse false → false; sparse true → true. Confirms no chart path runs on <30 candles.
  </action>
  <verify>
    <automated>pnpm test -- tests/ta/sparse-data.test.ts && grep -q "redirect" "src/app/ta/[ticker]/page.tsx" && grep -q "SparseDataCard" "src/app/ta/[ticker]/page.tsx" && grep -q "MobileInfoCard" "src/app/ta/[ticker]/page.tsx" && grep -q "api/ta/analysis" "src/app/ta/[ticker]/page.tsx" && grep -q "use client" src/app/ta/ta-chart-shell.tsx && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/ta/[ticker]/page.tsx` calls `redirect` for lowercase, renders `TAErrorCard`/`SparseDataCard`/`MobileInfoCard` for the respective states, and fetches `/api/ta/analysis/`
    - `src/app/ta/ta-chart-shell.tsx` is `"use client"` with default range "1Y" and overlay defaults `{BB:false,EMA20:false,EMA50:true,EMA200:true}`
    - `tests/ta/sparse-data.test.ts` asserts candle_count 10 → sparse render true, 250 → false
    - `pnpm build` succeeds (page + shell compile)
  </acceptance_criteria>
  <done>RSC page gates all four states (redirect/error/sparse/mobile) and mounts the live chart shell; landing page renders search; sparse gate unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: runTaRefreshOhlcv job + dispatcher cron + auth test</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <read_first>
    - src/app/api/internal/parse-batch/route.ts (auth + maxDuration + runtime + 508 comment lines 13-22)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (dispatch route section lines 486-540)
    - .planning/phases/13-t1-data-and-indicators/13-RESEARCH.md (Pattern 3 dispatcher direct-import + handleDaily)
    - src/lib/internal-auth.ts (Plan 01 — resolveCandidate, timingSafeStringEq)
    - src/lib/ta/fetch-ohlcv.ts + upsert-ohlcv.ts (Plan 03 — append-latest-bar)
    - src/lib/ingest/parse-document-batch.ts (runParseBatch signature { docId })
  </read_first>
  <action>
    1. Create `src/lib/ta/jobs/refresh-ohlcv.ts` (`import "server-only"`): `export async function runTaRefreshOhlcv(opts: { deadline: Date }): Promise<Record<string, unknown>>`. Read the seeded ticker list (from ticker_metadata via supabaseAdmin). For each ticker, while `Date.now() < opts.deadline.getTime()`: fetch the last ~5 trading days via `fetchOHLCV(ticker, fiveDaysAgo, today)` and `upsertOHLCV(ticker, bars)` (onConflict dedupes; appends only new bars). Log-and-continue per ticker. Return `{ tickersProcessed, skippedForDeadline }`.
    2. Adapt v1.0 jobs to the dispatcher: the existing `runParseBatch/runEmbedBatch/runAnalyzeBatch` take `{ docId }`, not `{ deadline }`. Create thin wrapper job-runners in the dispatcher (or a `src/lib/jobs/` helper) `runParseBatchSweep({ deadline })` that selects pending docIds from the documents table and loops `runParseBatch({ docId })` until done or deadline — mirroring how parse-batch/route.ts currently sweeps. Do NOT change the existing v1.0 job functions or their routes. Document this adapter in the SUMMARY.
    3. Create `src/app/api/internal/dispatch/route.ts`:
       - `export const maxDuration = 60; export const runtime = "nodejs";`
       - Auth: `const candidate = resolveCandidate(request); if (!timingSafeStringEq(candidate, env.INTERNAL_PARSE_SECRET)) return NextResponse.json({ error: "Unauthorized." }, { status: 401 });`
       - `jobSchema = z.enum(["daily","weekly"])` from `?job=`. 400 on unknown.
       - `daily`: `deadline = new Date(Date.now() + 55_000)`; sequentially run the v1.0 sweeps (parse/embed/analyze adapters) + `runTaRefreshOhlcv({ deadline })`, each guarded by a deadline check, each `.catch` capturing `{ error }`. Return `{ ok: true, results }`.
       - `weekly`: keep-alive — a trivial `supabaseAdmin.from("ticker_metadata").select("ticker").limit(1)` to keep Supabase warm (covers v1.0 R3 keep-alive intent partially; full R3 stays in backlog). Return `{ ok: true, kind: "weekly" }`.
       - `export function GET(request) { return handleDispatch(request); }`. Direct function imports ONLY — never `fetch()` self-call (508).
    4. Create `tests/ta/dispatcher-auth.test.ts` (TA-INFRA-02): import the route GET, build `Request` objects, assert 401 when `?secret=` is wrong/absent and 200 (or non-401) when correct. Mock the job functions and supabaseAdmin so no real work runs. Set `env.INTERNAL_PARSE_SECRET` for the test (vi mock of `@/lib/env`).
  </action>
  <verify>
    <automated>pnpm test -- tests/ta/dispatcher-auth.test.ts && grep -q "runTaRefreshOhlcv" src/app/api/internal/dispatch/route.ts && grep -q "resolveCandidate" src/app/api/internal/dispatch/route.ts && ! grep -q "fetch(" src/app/api/internal/dispatch/route.ts && grep -q "server-only" src/lib/ta/jobs/refresh-ohlcv.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/ta/jobs/refresh-ohlcv.ts` exports `runTaRefreshOhlcv({ deadline })`, imports `"server-only"`, and calls `fetchOHLCV` + `upsertOHLCV`
    - `dispatch/route.ts` uses `resolveCandidate` + `timingSafeStringEq`, enumerates job via Zod, imports job functions directly, and contains NO `fetch(` self-call
    - `tests/ta/dispatcher-auth.test.ts` asserts 401 without secret and non-401 with correct secret
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Refresh job appends latest bars within deadline; dispatcher routes daily/weekly via direct imports with timing-safe auth; auth test green.</done>
</task>

<task type="auto">
  <name>Task 3: vercel.json cutover + ONNX hello-world smoke scaffold</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <read_first>
    - vercel.json (current 2 crons to replace)
    - next.config.ts (current config — add outputFileTracingIncludes)
    - .planning/phases/13-t1-data-and-indicators/13-RESEARCH.md (vercel.json replacement lines 669-680; ONNX smoke Open Q4 protocol; anti-pattern: runtime="nodejs" required for onnxruntime-node)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (vercel.json section lines 795-819)
  </read_first>
  <action>
    1. REPLACE `vercel.json` crons with EXACTLY (hard cutover per RESEARCH.md Open Q3 recommendation — old routes remain valid endpoints but are no longer scheduled):
```json
{
  "crons": [
    { "path": "/api/internal/dispatch?job=daily",  "schedule": "0 11 * * *" },
    { "path": "/api/internal/dispatch?job=weekly", "schedule": "0 12 * * 0" }
  ]
}
```
       Do NOT put the secret in vercel.json. Add a Vercel project env note in the SUMMARY: the cron secret is delivered via Vercel's `CRON_SECRET` mechanism OR the dispatcher accepts the `?secret=` appended through a Vercel env-substituted cron path — confirm the exact Vercel delivery mechanism during the [BLOCKING] preview deploy (RESEARCH.md A5) and document it. If Vercel cannot append the secret to a committed path, the dispatcher must additionally accept Vercel's `Authorization: Bearer ${CRON_SECRET}` header (resolveCandidate already handles Bearer) — wire `CRON_SECRET` as an accepted secret alongside `INTERNAL_PARSE_SECRET` if needed.
    2. Install onnxruntime-node as a regular dependency (T3 will reuse it): `pnpm add onnxruntime-node`. (RESEARCH.md recommends regular dep over one-shot.)
    3. Create a ~5MB dummy `public/ta/dummy-model.onnx` (a minimal valid ONNX graph padded to ~5MB, OR generate via a tiny python/onnx export committed as a binary). If generating is impractical autonomously, create a placeholder and flag the binary creation for the [BLOCKING] task human step.
    4. Create `src/app/api/ta/onnx-smoke/route.ts`: `export const runtime = "nodejs"; export const maxDuration = 60;`. On GET, lazy-load `onnxruntime-node`, `InferenceSession.create("public/ta/dummy-model.onnx")` (resolve path via process.cwd()), run a trivial inference (or just session create if the dummy has no runnable graph), and return `{ ok: true, initMs: <time-to-session-create> }`. This route is for the cold-start measurement only.
    5. In `next.config.ts` add `outputFileTracingIncludes: { "/api/ta/onnx-smoke": ["./public/ta/dummy-model.onnx"] }` so the model is bundled into the function.
  </action>
  <verify>
    <automated>node -e "const c=require('./vercel.json'); if(c.crons.length!==2) throw new Error('expected 2 crons'); if(!c.crons.every(x=>x.path.includes('/api/internal/dispatch'))) throw new Error('crons not dispatcher'); console.log('crons ok')" && grep -q "onnxruntime-node" package.json && grep -q "runtime = \"nodejs\"" src/app/api/ta/onnx-smoke/route.ts && grep -q "outputFileTracingIncludes" next.config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `vercel.json` contains exactly 2 crons, both pointing at `/api/internal/dispatch` (daily + weekly), no secret in the file
    - `package.json` dependencies contains `onnxruntime-node`
    - `src/app/api/ta/onnx-smoke/route.ts` declares `runtime = "nodejs"` and lazy-loads onnxruntime-node
    - `next.config.ts` contains `outputFileTracingIncludes` referencing the dummy model
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>vercel.json cut over to the dispatcher pair (exactly 2 crons); ONNX smoke route + bundled dummy model + tracing config ready for preview measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Preview deploy — dispatcher cutover verify, ONNX INIT_DURATION, and E2E smoke</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <action>This is a human verification/action checkpoint — perform the steps in <how-to-verify> below and confirm every item in <acceptance_criteria>. No autonomous code change is made by this task.</action>
  <what-built>
    The full T1 surface + the cron cutover + ONNX smoke route. The dispatcher migration is a destructive one-shot (replaces v1.0 crons), and the ONNX measurement (TA-INFRA-04) requires a real Vercel preview deploy with cold-start log access — neither is doable autonomously. This also gathers the final integration evidence for VERIFICATION.md.
  </what-built>
  <how-to-verify>
    Deploy to a Vercel PREVIEW with `NEXT_PUBLIC_TA_ENABLED=true` and `INTERNAL_PARSE_SECRET` set. Then:

    1. Dispatcher auth (R1 close): 
       `curl -s -o /dev/null -w "%{http_code}" "{preview}/api/internal/dispatch?job=daily&secret=$INTERNAL_PARSE_SECRET"` → 200; same without `&secret=` → 401. Inspect the JSON: `results` includes `runTaRefreshOhlcv` and the v1.0 sweeps with non-error results. Confirm v1.0 pipeline still advances (no documents stuck in `parsing`).
    2. Confirm exactly 2 crons registered in the Vercel dashboard (Project → Settings → Cron Jobs). Confirm Vercel's secret-delivery mechanism (CRON_SECRET header vs ?secret=) and record which path authenticates the scheduled call; adjust env/wiring if the scheduled call 401s.
    3. ONNX smoke (TA-INFRA-04): wait ≥15 min since last invocation to force cold start, then `curl {preview}/api/ta/onnx-smoke` 5 times in sequence. Read Vercel Function logs for INIT_DURATION on the first (cold) invocation. Record all 5 INIT_DURATION values. If INIT_DURATION > 5000ms on ≥3 of 5 cold curls, flag T3 architecture for revisit. Write the measured values into `.planning/phases/13-t1-data-and-indicators/13-VERIFICATION.md`.
    4. E2E browser smoke: visit /ta/BBCA, /ta/TLKM, /ta/GOTO on preview — full chart + subpanels + range + overlays + snapshot render. Visit /ta/bbca → 301 to /ta/BBCA. Visit /ta/ZZZZ (not in metadata) → "Ticker not found" card. Resize to 375px → MobileInfoCard. 
    5. Run full suite: `pnpm test` green.
  </how-to-verify>
  <acceptance_criteria>
    - dispatcher returns 200 with correct secret, 401 without; v1.0 pipeline confirmed still running
    - exactly 2 Vercel crons exist; the scheduled call authenticates (no 401 on the real cron trigger)
    - ONNX cold INIT_DURATION (5 values) recorded in 13-VERIFICATION.md with the >5s flag decision
    - /ta/BBCA, /ta/TLKM, /ta/GOTO render; lowercase 301; invalid → error card; 375px → mobile card
    - `pnpm test` exits 0
  </acceptance_criteria>
  <resume-signal>Type "verified" with the INIT_DURATION values and dispatcher http codes, or describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron / external client → /api/internal/dispatch | Unauthenticated scheduled + manual calls; secret is the only gate |
| Browser → /ta/[ticker] RSC → /api/ta/analysis | Public ticker param flows server-side |
| onnxruntime-node native module → Vercel function | Native dependency cold-load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-25 | Spoofing | unauthorized dispatcher invocation | mitigate | `resolveCandidate` + `timingSafeStringEq(env.INTERNAL_PARSE_SECRET)`; 401 on mismatch. Verified by dispatcher-auth.test.ts + preview curl. Closes v1.0 R1 (single agreed auth path). |
| T-13-26 | Denial of Service | dispatcher 508 INFINITE_LOOP self-fetch | mitigate | Direct function imports only; verify NO `fetch(` in dispatch route (grep gate). 55s deadline budget bounds each job. |
| T-13-27 | Denial of Service | one job overruns 60s and starves others | mitigate | `deadline = now + 55s`; each job checks deadline before running and self-limits; overrun returns `{ skipped: "deadline" }` not a crash. |
| T-13-28 | Tampering | secret committed in vercel.json | mitigate | Secret NEVER in vercel.json (committed); delivered via Vercel CRON_SECRET/env at trigger time; confirmed during preview deploy. |
| T-13-29 | Tampering | ticker path param injection on RSC route | mitigate | `normalizeTickerParam` validates `/^[A-Z]{1,5}$/` before any fetch; invalid → error card, no interpolation into external calls. |
| T-13-30 | Denial of Service | onnxruntime-node cold-start blows function budget | mitigate→measure | TA-INFRA-04 smoke MEASURES cold INIT_DURATION; >5s on ≥3/5 flags T3 architecture revisit before Phase 15 commits. runtime="nodejs" declared (native dep requirement). |
</threat_model>

<verification>
- `pnpm test` full suite green (sparse-data, dispatcher-auth, plus all prior TA tests)
- `pnpm build` succeeds
- vercel.json has exactly 2 dispatcher crons; dispatch route has no self-fetch; direct imports present
- [BLOCKING] preview: dispatcher 200/401 correct, v1.0 pipeline intact, ONNX INIT_DURATION recorded in VERIFICATION.md, /ta/{BBCA,TLKM,GOTO} render, lowercase 301, invalid/mobile states correct
</verification>

<success_criteria>
- /ta/{TICKER} renders the full cached chart surface; all four gate states correct; lowercase 301s
- Exactly 2 Vercel crons (dispatcher daily + weekly); direct-import jobs incl. runTaRefreshOhlcv; v1.0 pipeline preserved; R1 implicitly closed
- ONNX hello-world cold INIT_DURATION measured and recorded; >5s decision documented
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/13-t1-data-and-indicators/13-07-SUMMARY.md` (record the v1.0-job dispatcher adapter, the Vercel secret-delivery mechanism, and the ONNX INIT_DURATION values).
</output>
