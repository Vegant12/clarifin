---
phase: 13-t1-data-and-indicators
plan: 05
type: execute
wave: 2
depends_on: [13-01, 13-03]
files_modified:
  - src/components/ta/candlestick-chart.tsx
  - src/components/ta/indicator-subpanel.tsx
  - src/components/ta/range-selector.tsx
  - src/components/ta/overlay-toggles.tsx
  - src/components/ta/indicator-snapshot-strip.tsx
  - src/components/ta/indicator-tooltip.tsx
  - src/components/ta/chart-types.ts
autonomous: false
requirements: [TA-CHART-01, TA-CHART-02, TA-CHART-03, TA-CHART-04, TA-CHART-05, TA-CHART-06, TA-IND-01, TA-IND-02, TA-IND-03, TA-IND-04, TA-IND-05, TA-IND-06, TA-IND-07]
must_haves:
  truths:
    - "An interactive candlestick chart renders OHLC with up/down coloring, zoom/pan, crosshair, and hover tooltip"
    - "Volume, RSI(14) with 30/70 lines, and MACD(12,26,9) histogram render as separate synced subpanels"
    - "Range selector (1M/3M/6M/1Y/2Y) filters loaded data client-side; default 1Y"
    - "Bollinger Bands + EMA-20/50/200 toggle as overlays (EMA-50/200 ON, EMA-20/BB OFF by default)"
    - "Indicator snapshot strip renders plain-English one-liners with per-indicator hover hint"
  artifacts:
    - path: "src/components/ta/candlestick-chart.tsx"
      provides: "lightweight-charts main panel + subpanel orchestration with synced time axis"
      contains: "subscribeVisibleTimeRangeChange"
    - path: "src/components/ta/indicator-snapshot-strip.tsx"
      provides: "TA-IND-05 plain-English one-liner cards with TA-IND-06 tooltips"
      contains: "snapshot"
  key_links:
    - from: "src/components/ta/candlestick-chart.tsx"
      to: "lightweight-charts"
      via: "createChart in useEffect with cleanup"
      pattern: "createChart"
    - from: "src/components/ta/candlestick-chart.tsx"
      to: "indicator-subpanel.tsx"
      via: "synced via subscribeVisibleTimeRangeChange + isSyncing guard"
      pattern: "isSyncing"
---

<objective>
Build the client-side chart subtree: the lightweight-charts candlestick main panel with synced volume/RSI/MACD subpanels, range selector, overlay toggles, and the indicator-snapshot strip with hover hints. All components are `"use client"` and consume serialized props (OHLCV + indicators + snapshot) — no data fetching here. Plan 07 mounts this subtree inside the RSC page.

Purpose: The user-visible TradingView-style surface (D-01). Subpanel sync and SSR-safe chart lifecycle are the two highest-risk areas (Pitfalls 1 and 6).
Output: Seven client components plus a shared chart-types module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-t1-data-and-indicators/13-RESEARCH.md
@.planning/phases/13-t1-data-and-indicators/13-PATTERNS.md
@.planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md

<interfaces>
Props from Plan 04 AnalysisPayload (Plan 07 passes these into CandlestickChart):
- ohlcv: array of { date, open, high, low, close, adjClose, volume } (ascending)
- indicators: IndicatorSet { rsi, macd, bollingerBands, ema20, ema50, ema200, sma50, atr, stochastic, obv } (each aligned to ohlcv length, null-padded)
- snapshot: Record string to string (plain-English one-liners)

lightweight-charts 5.2.0 (VERIFY v5 API before coding, RESEARCH.md Open Q1; v4 tutorials differ):
- createChart(el, options) returns IChartApi
- series creation: chart.addCandlestickSeries(opts) (v4) OR chart.addSeries(CandlestickSeries, opts) (v5) — confirm against installed package types
- series.setData with time = "YYYY-MM-DD" string
- chart.timeScale().subscribeVisibleTimeRangeChange(cb); .setVisibleRange(range); .getVisibleRange()
- series.applyOptions({ visible: boolean })
- chart.remove()

CHART_COLORS (UI-SPEC): up=var(--color-primary) emerald-600, down=var(--color-destructive) red-600,
borderUp #047857, borderDown #b91c1c, rsiLine #3b82f6, macdFast #6366f1, macdSignal #f97316,
EMA20 #60a5fa, EMA50 #fbbf24, EMA200 #8b5cf6, BB #71717a at 40% opacity.
Subpanel heights: main 300px, volume 80px, RSI 100px, MACD 120px.
shadcn Button (variant outline/default/ghost), Card, Tooltip already installed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify lightweight-charts v5 API, define chart-types, build candlestick chart + synced subpanels</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <read_first>
    - src/components/doc/trend-chart-card.tsx ("use client" + chart container pattern in the codebase)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (candlestick-chart section, lines 613-691 — useEffect/ref/cleanup + subpanel sync verbatim)
    - .planning/phases/13-t1-data-and-indicators/13-RESEARCH.md (Pattern 1 subpanel sync, Pattern 6 SSR null-ref, Open Q1 v5 API)
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (CandlestickChart interaction contract, lines 224-242)
    - node_modules/lightweight-charts package exported types (confirm v5 method names BEFORE writing series code)
  </read_first>
  <action>
    1. FIRST verify the installed lightweight-charts@5.2.0 API: run `node -e "const lc=require('lightweight-charts'); console.log(Object.keys(lc))"` and inspect the package types to confirm whether series use `chart.addCandlestickSeries(...)` (v4) or `chart.addSeries(CandlestickSeries, ...)` (v5). Use the actual installed API. Record the confirmed API in the SUMMARY for Plan 07 and T2.
    2. Create `src/components/ta/chart-types.ts` (pure types + const, NOT "use client"): export `ChartOHLCV`, `RangeKey = "1M"|"3M"|"6M"|"1Y"|"2Y"`, `OverlayKey = "BB"|"EMA20"|"EMA50"|"EMA200"`, `const CHART_COLORS` from the UI-SPEC values, and `RANGE_TO_DAYS: Record<RangeKey, number>` (1M=21, 3M=63, 6M=126, 1Y=252, 2Y=504).
    3. Create `src/components/ta/indicator-subpanel.tsx` (`"use client"`): a presentational wrapper rendering a `<div ref>` of given height, exposing `onChartReady(chart, ref)` so the parent wires sync. It creates its own chart in `useEffect` (SSR guard `if (!ref.current) return;`), creates the series for its `kind` ("volume"|"rsi"|"macd"), sets data from props, returns `chart.remove()` in cleanup. RSI: two horizontal price-lines at 30/70 (zinc-400 dashed). MACD: histogram (emerald positive / red negative) + fast/signal lines. Volume: bars emerald/red by up/down day at 60% opacity.
    4. Create `src/components/ta/candlestick-chart.tsx` (`"use client"`), the orchestrator. In a single `useEffect([ohlcv, indicators])`:
       - SSR guard on the main ref.
       - Create main chart (300px), candlestick series with CHART_COLORS, crosshair mode normal, grid `--color-border`.
       - Add EMA-20/50/200 + Bollinger line series as overlays; set data from `indicators` (map null values to omitted points / whitespace gaps). Default visibility: EMA-50/200 visible, EMA-20/BB hidden via `series.applyOptions({ visible })`. Keep series references so OverlayToggles can flip visibility (lift a `seriesRefs` object to component state/ref).
       - Render volume/RSI/MACD via three `<IndicatorSubpanel>` children; collect their IChartApi refs.
       - Wire subpanel sync EXACTLY per PATTERNS.md with the mandatory `isSyncing` guard: `let isSyncing=false; function syncFrom(source, charts){ if(isSyncing) return; isSyncing=true; const r=source.timeScale().getVisibleRange(); if(r) charts.filter(c=>c!==source).forEach(c=>c.timeScale().setVisibleRange(r)); isSyncing=false; }` and subscribe all four charts via `subscribeVisibleTimeRangeChange`. Without the guard the tab freezes (Pitfall 1).
       - Custom hover tooltip: subscribe to crosshair move, render a floating div (`bg-background border border-border rounded-md shadow-sm px-3 py-2 text-sm`) with date + OHLCV + volume (TA-CHART-04), matching v1.0 TrendTooltip styling.
       - Wrapper `role="img"` `aria-label="{ticker} interactive price chart, {range} view"`.
    5. Range filtering: the parent (Plan 07 / RangeSelector) passes already-sliced `ohlcv`/`indicators` for the active range; the chart re-runs its effect on prop change.
  </action>
  <verify>
    <automated>grep -q "subscribeVisibleTimeRangeChange" src/components/ta/candlestick-chart.tsx && grep -q "isSyncing" src/components/ta/candlestick-chart.tsx && grep -q "createChart" src/components/ta/candlestick-chart.tsx && grep -q "chart.remove" src/components/ta/candlestick-chart.tsx && grep -q "CHART_COLORS" src/components/ta/chart-types.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ta/candlestick-chart.tsx` starts with `"use client"`, contains `createChart(`, `subscribeVisibleTimeRangeChange`, `isSyncing`, and a `chart.remove()` cleanup
    - `src/components/ta/indicator-subpanel.tsx` contains `"use client"` and creates charts inside `useEffect`
    - `src/components/ta/chart-types.ts` exports `CHART_COLORS`, `RangeKey`, `RANGE_TO_DAYS`
    - candlestick-chart adds EMA + Bollinger overlay series and applies default visibility (EMA-50/200 on, EMA-20/BB off)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Candlestick chart renders OHLC with overlays, crosshair, tooltip, zoom/pan; volume/RSI/MACD subpanels stay time-synced via guarded subscription; charts dispose on unmount.</done>
</task>

<task type="auto">
  <name>Task 2: Range selector + overlay toggles + snapshot strip + indicator tooltip</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <read_first>
    - src/components/chat/starter-questions.tsx (chip/button interaction + cn() pattern)
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (RangeSelector lines 208-215, OverlayToggles lines 217-222, IndicatorSnapshotStrip lines 244-258, IndicatorTooltip line 118)
    - src/components/ta/chart-types.ts (Task 1 — RangeKey, OverlayKey, CHART_COLORS)
    - src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/tooltip.tsx (shadcn primitives)
  </read_first>
  <action>
    1. Create `src/components/ta/range-selector.tsx` (`"use client"`): 5 shadcn `<Button size="sm">` for 1M/3M/6M/1Y/2Y. Active button `variant="default"` (filled emerald) with `aria-pressed="true"`; inactive `variant="outline"`. Props: `value: RangeKey`, `onChange: (r: RangeKey) => void`. Default selection handled by parent (1Y).
    2. Create `src/components/ta/overlay-toggles.tsx` (`"use client"`): 4 chip toggles BB / EMA-20 / EMA-50 / EMA-200. Props: `active: Record<OverlayKey, boolean>`, `onToggle: (k: OverlayKey) => void`. On-state: `variant="outline"` + 3px left border in the series color (inline `borderLeftColor` from CHART_COLORS) + `bg-muted`. Off-state: `variant="ghost"`. Labels per copywriting contract: "Bollinger Bands" / "EMA 20" / "EMA 50" / "EMA 200". Defaults (EMA-50/200 on) owned by parent state.
    3. Create `src/components/ta/indicator-tooltip.tsx` (`"use client"`): shadcn `<Tooltip>` wrapper showing a static plain-English definition for a given indicator key (TA-IND-06). Include a `INDICATOR_DEFINITIONS: Record<string,string>` const (e.g. RSI: "Relative Strength Index — momentum from 0-100; under 30 often oversold, over 70 often overbought."). Trigger accessible on focus + hover; screen-reader label "What is {indicatorName}?".
    4. Create `src/components/ta/indicator-snapshot-strip.tsx` (`"use client"`): horizontal strip of `<Card px-3 py-2>` chips, one per indicator key present in `snapshot`. Each chip: `<p class="text-xs font-semibold text-muted-foreground">{label}</p>` + `<p class="text-sm text-foreground">{snapshot[key]}</p>`, wrapped in `<IndicatorTooltip>`. Props: `snapshot: Record<string,string>`. `flex-wrap` on wide screens, `overflow-x-auto` when narrow. Min-width 120px per chip.
    5. These four components are presentational/state-driven only — no lightweight-charts imports. Overlay visibility is applied by the parent calling `series.applyOptions({visible})` on the refs from Task 1; OverlayToggles only reports toggle intent.
  </action>
  <verify>
    <automated>grep -q "aria-pressed" src/components/ta/range-selector.tsx && grep -q "borderLeftColor" src/components/ta/overlay-toggles.tsx && grep -q "snapshot" src/components/ta/indicator-snapshot-strip.tsx && grep -q "INDICATOR_DEFINITIONS" src/components/ta/indicator-tooltip.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `range-selector.tsx` renders exactly 5 buttons (1M/3M/6M/1Y/2Y) and sets `aria-pressed` on the active one
    - `overlay-toggles.tsx` renders 4 chips with colored left border on active state via `borderLeftColor`
    - `indicator-snapshot-strip.tsx` reads `snapshot[key]` and wraps each chip in `IndicatorTooltip`
    - `indicator-tooltip.tsx` exports `INDICATOR_DEFINITIONS` and renders an accessible shadcn Tooltip
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Range selector, overlay toggles, snapshot strip, and indicator tooltips render per UI-SPEC; all state-driven and ready for Plan 07 wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser smoke of the chart subtree (preview)</name>
  <files>See file paths enumerated in the task action and this plan's files_modified frontmatter.</files>
  <action>This is a human verification/action checkpoint — perform the steps in <how-to-verify> below and confirm every item in <acceptance_criteria>. No autonomous code change is made by this task.</action>
  <what-built>
    The full client chart subtree: candlestick main panel, synced volume/RSI/MACD subpanels, range selector, overlay toggles, snapshot strip. lightweight-charts is canvas-rendered with no jsdom/Playwright in T1 scope, so visual correctness needs a human.
  </what-built>
  <how-to-verify>
    This needs Plan 07's page wiring + seeded data to render end-to-end, so run this verification during/after Plan 07 on a Vercel preview deploy (NEXT_PUBLIC_TA_ENABLED=true in preview):
    1. Visit /ta/BBCA on the preview URL.
    2. Confirm: candlesticks render green-up / red-down; volume, RSI (with 30/70 lines), MACD (with histogram) subpanels visible below.
    3. Pan/zoom the main chart with wheel + drag — all four panels move together, no tab freeze (validates isSyncing guard).
    4. Hover a candle — tooltip shows date + OHLCV + volume.
    5. Click 1M/3M/6M/1Y/2Y — chart re-ranges; 1Y is the default on load.
    6. Toggle BB and EMA-20 on, EMA-50 off — overlays appear/disappear accordingly.
    7. Hover an indicator chip — plain-English definition tooltip appears; chip text is a one-liner, never a bare number triplet.
  </how-to-verify>
  <acceptance_criteria>
    - All 4 panels render and stay time-synced during pan/zoom with no freeze
    - Range selector, overlay toggles, hover tooltip, and snapshot tooltips all function as described
    - Snapshot chips show plain-English one-liners (no raw "1.23 / 0.98 / 0.25")
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe rendering/sync issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server props (AnalysisPayload) → client chart components | Data already validated server-side; client renders only |
| lightweight-charts canvas → DOM | Third-party rendering library in the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Denial of Service | subpanel sync infinite loop freezes tab | mitigate | Mandatory `isSyncing` re-entrancy guard around every `setVisibleRange` cross-call (Pitfall 1). Verified in browser smoke (Task 3, step 3). |
| T-13-18 | Denial of Service | SSR null-ref crash on createChart | mitigate | All `createChart` calls inside `useEffect` with `if (!ref.current) return;` guard and `chart.remove()` cleanup (Pitfall 6). |
| T-13-19 | Tampering | rendering NaN/null indicator points | mitigate | Null-padded indicator values mapped to omitted points (whitespace), never NaN; sparse data gated upstream by Plan 04/07 before this subtree mounts. |
| T-13-20 | Information Disclosure | no server secrets in client bundle | mitigate | Components are pure presentational; import only lightweight-charts + shadcn + chart-types; no env/secret imports. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0
- candlestick-chart contains createChart, subscribeVisibleTimeRangeChange, isSyncing, chart.remove cleanup
- range-selector aria-pressed; overlay-toggles borderLeftColor; snapshot-strip reads snapshot; indicator-tooltip definitions
- [browser smoke, with Plan 07] 4 synced panels, range/toggle/tooltip functional, no freeze
</verification>

<success_criteria>
- Interactive candlestick + volume/RSI/MACD subpanels render and stay synced
- Range selector (default 1Y) re-filters client-side; overlays toggle (EMA-50/200 default on)
- Snapshot strip shows plain-English one-liners with definition tooltips
- No SSR crash, no sync freeze, charts dispose cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/13-t1-data-and-indicators/13-05-SUMMARY.md` (record the confirmed lightweight-charts v5 series-creation API for Plan 07 + T2).
</output>
