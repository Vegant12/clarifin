---
phase: 13-t1-data-and-indicators
plan: 06
type: execute
wave: 2
depends_on: [13-01, 13-04]
files_modified:
  - src/components/site-header.tsx
  - src/components/ta/ticker-search.tsx
  - src/components/ta/ta-page-skeleton.tsx
  - src/components/ta/ta-error-card.tsx
  - src/components/ta/sparse-data-card.tsx
  - src/components/ta/mobile-info-card.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [TA-UX-01, TA-TICKER-01, TA-CHART-07, TA-CHART-08]
must_haves:
  truths:
    - "A shared SiteHeader is mounted in RootLayout with Upload Document and a TA Analysis link gated on NEXT_PUBLIC_TA_ENABLED"
    - "TickerSearch debounces input, queries /api/ta/search, and routes to /ta/{TICKER} uppercase on select"
    - "Invalid tickers show a friendly error card; in-flight loads show a skeleton; <30 candles shows a sparse-data card"
    - "Viewports <640px show a clean 'best on desktop' info card instead of a broken chart"
  artifacts:
    - path: "src/components/site-header.tsx"
      provides: "shared nav with conditional TA link (TA-UX-01)"
      contains: "NEXT_PUBLIC_TA_ENABLED"
    - path: "src/components/ta/ticker-search.tsx"
      provides: "debounced autocomplete routing to /ta/{TICKER}"
      contains: "/api/ta/search"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/site-header.tsx"
      via: "mounted above SessionProvider in RootLayout"
      pattern: "SiteHeader"
    - from: "src/components/ta/ticker-search.tsx"
      to: "/api/ta/search"
      via: "debounced fetch in useEffect"
      pattern: "/api/ta/search"
---

<objective>
Build the navigation + supporting UI states: the shared `<SiteHeader />` mounted in RootLayout (TA-UX-01, TA link gated on `NEXT_PUBLIC_TA_ENABLED` per D-04), the debounced `<TickerSearch />` autocomplete (TA-TICKER-01), and the four page-state cards (skeleton, invalid-ticker error, sparse-data, mobile "best on desktop" per D-05). Plan 07 composes these into the page.

Purpose: These surround the chart with discoverability, search, and graceful states. Mounting SiteHeader in RootLayout affects every route, so it must render identically for non-TA pages when the flag is off.
Output: Six components + the layout modification, plus the new env var declaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-t1-data-and-indicators/13-PATTERNS.md
@.planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md
@.planning/phases/13-t1-data-and-indicators/13-CONTEXT.md

<interfaces>
Env: add `NEXT_PUBLIC_TA_ENABLED` (client) to src/lib/env.ts and its runtimeEnv map. Default behavior when unset = link hidden. Set true in Vercel preview env; false in prod until VERIFICATION.md lands (D-04).

Search API (Plan 04): GET /api/ta/search?q={q}&limit=8 returns { results: [{ ticker, name_en, name_id, sector }] }.
Ticker normalize helper (Plan 04): src/lib/ta/ticker-route.ts exports normalizeTickerParam — but TickerSearch routes only to uppercase tickers returned by the API, so it uses router.push(`/ta/${result.ticker}`) directly.

Layout (current src/app/layout.tsx): RootLayout renders SessionProvider then OnboardingModal inside body. SiteHeader mounts ABOVE SessionProvider.

shadcn primitives available: Command, CommandInput, CommandList, Card, Button, Skeleton, Tooltip, Separator.
Copywriting (UI-SPEC lines 302-325) is LOCKED — use the exact strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SiteHeader + env flag + RootLayout mount</name>
  <read_first>
    - src/app/layout.tsx (current RootLayout structure — where to mount SiteHeader)
    - src/lib/env.ts (client schema + runtimeEnv map — where to add NEXT_PUBLIC_TA_ENABLED)
    - src/components/onboarding-modal.tsx ("use client" component mounted in layout pattern)
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (SiteHeader contract lines 289-296, copywriting)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (site-header section lines 738-791)
  </read_first>
  <action>
    1. In `src/lib/env.ts`: add to the `client` schema `NEXT_PUBLIC_TA_ENABLED: z.string().optional()` and add `NEXT_PUBLIC_TA_ENABLED: process.env.NEXT_PUBLIC_TA_ENABLED` to the `runtimeEnv` map. (String, not boolean — Next.js NEXT_PUBLIC vars are strings; compare `=== "true"`.)
    2. Create `src/components/site-header.tsx` (`"use client"`): a `<header role="banner">` `sticky top-0 z-50 h-12 bg-secondary border-b border-border`. Left: "Clarifin" wordmark `text-sm font-semibold` linking to `/`. Right nav: "Upload Document" as `<Button variant="ghost" asChild><Link href="/">`. "TA Analysis" link rendered only when `process.env.NEXT_PUBLIC_TA_ENABLED === "true"` — as `<Button variant="ghost" asChild><Link href="/ta">`. Active state on `/ta/*` (use `usePathname()`): `text-primary` / underline. On `<640px` the "TA Analysis" label is icon-only (`lucide-react` `<LineChart>`) with `aria-label="TA Analysis"`. Use `cn()`.
    3. Modify `src/app/layout.tsx`: import `SiteHeader` and render `<SiteHeader />` ABOVE `<SessionProvider>` inside `<body>` (so it is outside the provider, per UI-SPEC, avoiding hydration issues). Do not otherwise change the layout.
    4. Verify the header renders on existing non-TA routes with the TA link hidden when the flag is unset (default).
  </action>
  <verify>
    <automated>grep -q "NEXT_PUBLIC_TA_ENABLED" src/lib/env.ts && grep -q "NEXT_PUBLIC_TA_ENABLED" src/components/site-header.tsx && grep -q "SiteHeader" src/app/layout.tsx && grep -q "role=\"banner\"" src/components/site-header.tsx && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/env.ts` client schema and runtimeEnv both contain `NEXT_PUBLIC_TA_ENABLED`
    - `src/components/site-header.tsx` gates the TA link on `process.env.NEXT_PUBLIC_TA_ENABLED === "true"` and contains `role="banner"`, "Upload Document", and "TA Analysis"
    - `src/app/layout.tsx` renders `<SiteHeader />` above `<SessionProvider>`
    - `pnpm typecheck` exits 0 and `pnpm build` succeeds (layout change compiles for all routes)
  </acceptance_criteria>
  <done>SiteHeader mounted globally; TA link appears only when the env flag is "true"; existing routes unaffected when off.</done>
</task>

<task type="auto">
  <name>Task 2: TickerSearch debounced autocomplete</name>
  <read_first>
    - src/components/chat/starter-questions.tsx (client interaction + chip styling)
    - src/components/ui/command.tsx (shadcn Command/CommandInput/CommandList API)
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (TickerSearch contract lines 196-206, copywriting)
    - .planning/phases/13-t1-data-and-indicators/13-PATTERNS.md (ticker-search section lines 694-734)
  </read_first>
  <action>
    1. Create `src/components/ta/ticker-search.tsx` (`"use client"`) using shadcn `<Command>` + `<CommandInput>` + `<CommandList>`:
       - State: `query` (string), `results` (array), `loading` (bool).
       - Debounce 300ms: in a `useEffect([query])`, if `query.length < 1` clear results and fire no request; else `setTimeout(() => fetch('/api/ta/search?q=' + encodeURIComponent(query) + '&limit=8'))`, parse `json.results`. Clear the timer in cleanup.
       - Result row (40px): `[TICKER]` `text-sm font-mono font-semibold` + `name_en` `text-sm text-muted-foreground`.
       - On select: `useRouter().push('/ta/' + result.ticker)` (ticker is already uppercase from the API). Keyboard: arrows navigate, Enter selects, Escape closes (Command provides this).
       - Empty results: "No IDX tickers matching '{query}'" (`text-sm text-muted-foreground py-4 px-3`).
       - Input placeholder: "Search IDX tickers or company names…". `aria-label="Search IDX tickers"`, `aria-expanded` on open.
    2. This component does NOT import the chart or any server module — pure client + fetch.
  </action>
  <verify>
    <automated>grep -q "/api/ta/search" src/components/ta/ticker-search.tsx && grep -q "setTimeout" src/components/ta/ticker-search.tsx && grep -q "push(" src/components/ta/ticker-search.tsx && grep -q "Search IDX tickers" src/components/ta/ticker-search.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ticker-search.tsx` fetches `/api/ta/search` with a 300ms `setTimeout` debounce cleared in cleanup
    - routes via `router.push('/ta/' + result.ticker)` (uppercase from API)
    - contains the locked placeholder "Search IDX tickers or company names…" and the empty-state copy
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Debounced autocomplete queries the search API, renders code+name rows, and routes to the uppercase ticker page on select.</done>
</task>

<task type="auto">
  <name>Task 3: State cards — skeleton, error, sparse, mobile</name>
  <read_first>
    - src/components/ui/card.tsx, src/components/ui/skeleton.tsx, src/components/ui/button.tsx
    - .planning/phases/13-t1-data-and-indicators/13-UI-SPEC.md (skeleton lines 260-269, error lines 271-278, sparse lines 280-287, mobile card line 120 + copywriting lines 309-321)
  </read_first>
  <action>
    1. `src/components/ta/ta-page-skeleton.tsx`: shadcn `<Skeleton>` blocks — `w-full h-8` (search), gap 16px, `w-full h-[600px]` (chart area), gap 16px, `w-full h-16` (indicator strip). All `animate-pulse bg-muted`. Wrapper `role="status" aria-label="Loading chart data…" aria-busy="true"`.
    2. `src/components/ta/ta-error-card.tsx`: props `{ ticker: string }`. Centered `<Card max-w-md mx-auto mt-16>`. `lucide-react <AlertCircle>` size-8 text-destructive. Heading "Ticker not found" `text-xl font-semibold`. Body (locked): "'{TICKER}' is not in our list of IDX-listed companies. Check the spelling, or search by company name above." CTA `<Button variant="outline">` "Search again" linking to `/ta`. (Reuse this card for the generic fetch-error too via an optional `variant` prop: heading "Could not load chart data", body the fetch-error copy, CTA "Try again".)
    3. `src/components/ta/sparse-data-card.tsx`: props `{ ticker: string }`. Centered Card. `lucide-react <BarChart2>` size-8 text-muted-foreground. Heading "Insufficient price history". Body (locked): "Technical indicators need at least 30 trading days of data to be reliable. '{TICKER}' was recently listed and doesn't have enough history yet. Check back in a few weeks." No chart/indicators.
    4. `src/components/ta/mobile-info-card.tsx`: Card shown only `<640px` (caller wraps with `block sm:hidden`). `lucide-react <Monitor>` icon. Heading "TA Analysis works best on desktop". Body (locked): the full mobile-info body string from UI-SPEC line 310. CTA `<Button asChild>` "Upload a document instead" linking to `/` (D-05).
    5. All cards use the `section/Card + cn()` structure; copywriting strings must match UI-SPEC EXACTLY (verbatim).
  </action>
  <verify>
    <automated>grep -q "aria-busy" src/components/ta/ta-page-skeleton.tsx && grep -q "Ticker not found" src/components/ta/ta-error-card.tsx && grep -q "Insufficient price history" src/components/ta/sparse-data-card.tsx && grep -q "works best on desktop" src/components/ta/mobile-info-card.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - all four card files exist; copy strings match UI-SPEC verbatim ("Ticker not found", "Insufficient price history", "TA Analysis works best on desktop", "Upload a document instead")
    - `ta-page-skeleton.tsx` has `role="status"` and `aria-busy="true"`
    - error and sparse cards accept a `ticker` prop and interpolate it into the body
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Four graceful state cards render with locked copy; ready for Plan 07 to gate on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/ta/search (via TickerSearch) | User-typed query sent to backend |
| NEXT_PUBLIC_TA_ENABLED flag → global header render | Controls public discoverability of unfinished surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-21 | Information Disclosure | TA surface exposed in prod before ready | mitigate | TA link gated on `NEXT_PUBLIC_TA_ENABLED === "true"`; false in prod until VERIFICATION.md lands (D-04). Route still reachable by URL but unlinked; full prod exposure is a Phase 16 concern. |
| T-13-22 | Spoofing | open redirect via search result | mitigate | `router.push('/ta/' + result.ticker)` uses only the API-returned uppercase ticker (sourced from ticker_metadata), never raw user input. |
| T-13-23 | Denial of Service | search request per keystroke | mitigate | 300ms debounce + min-length 1; cleanup clears pending timer; limit=8. |
| T-13-24 | Tampering | XSS via ticker/company name in results | mitigate | React escapes interpolated text by default; no dangerouslySetInnerHTML; copy strings are static. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0; `pnpm build` succeeds (global layout change)
- env.ts declares NEXT_PUBLIC_TA_ENABLED; SiteHeader gates link; mounted in layout
- ticker-search debounces, hits /api/ta/search, routes to uppercase ticker
- four state cards exist with verbatim locked copy
</verification>

<success_criteria>
- SiteHeader globally mounted; TA link conditional on env flag; existing routes unaffected
- Debounced autocomplete routes to /ta/{TICKER}
- Skeleton, error, sparse, and mobile cards render with locked copy for Plan 07 to compose
</success_criteria>

<output>
After completion, create `.planning/phases/13-t1-data-and-indicators/13-06-SUMMARY.md`.
</output>
