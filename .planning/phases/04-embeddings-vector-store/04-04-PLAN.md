---
phase: 04-embeddings-vector-store
plan: 04
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - src/lib/ingest/trigger-parse-batch.ts
  - src/lib/ingest/parse-document-batch.ts
  - src/app/api/internal/embed-batch/route.ts
  - vercel.json
  - .env.example
autonomous: true
requirements:
  - INGEST-06
must_haves:
  truths:
    - "scheduleEmbedBatchesForDoc(docId) POSTs to /api/internal/embed-batch with Bearer INTERNAL_PARSE_SECRET; skips when NODE_ENV === test (mirror parse)"
    - "When parse completes and sets documents.status = embedding, scheduleEmbedBatchesForDoc(docId) runs so local dev advances without waiting for cron"
    - "When embed-batch returns done: false, scheduleEmbedBatchesForDoc(docId) re-queues via after() like parse-batch"
    - "vercel.json lists cron path /api/internal/embed-batch with ?secret= documented in .env.example comment"
  artifacts:
    - path: "src/lib/ingest/trigger-parse-batch.ts"
      provides: "scheduleEmbedBatchesForDoc"
    - path: "vercel.json"
      provides: "crons[] includes embed-batch"
  key_links:
    - from: "parse-document-batch (status embedding)"
      to: "scheduleEmbedBatchesForDoc"
      via: "import + call"
      pattern: "scheduleEmbed"
---

<objective>
Wire **continuation scheduling** for embedding (**D4-03**): **`after()`** fetches to **`/api/internal/embed-batch`** with **`INTERNAL_PARSE_SECRET`**, mirroring **`scheduleParseBatchesForDoc`**. Invoke when parsing flips to **`embedding`**, and from **`embed-batch`** when **`done: false`**. Add **Vercel Cron** for embed worker.

Purpose: Production and local (no cron gap) both advance documents through embedding without manual hits.

Output: Updated trigger module, parse completion hook, embed route scheduling, **`vercel.json`** + **`.env.example`** cron note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/ingest/trigger-parse-batch.ts
@src/lib/ingest/parse-document-batch.ts (tail where status → embedding)
@src/app/api/internal/embed-batch/route.ts
@vercel.json
</context>

## Source coverage (this plan)

| Source | Items | Covered |
|--------|-------|---------|
| CONTEXT | D4-03 cron + after() | Tasks |
| PATTERNS.md | after() scheduling | trigger file |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server `after()` → internal URL | Carries **`Bearer`** secret — not client-visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-a | I | Secret in URL logs | mitigate | **`scheduleEmbedBatchesForDoc`** uses **`Authorization: Bearer`** header (not query) for **`after()`** server fetch; **cron** uses **`?secret=`** (Vercel limitation) — document dashboard-only URL config |
| T-04-04-b | I | Unauthenticated embed | mitigate | Cron path **`?secret=`** equals **`INTERNAL_PARSE_SECRET`** — same threat model as **parse-batch** **`parse-batch` route.ts** |
| T-04-04-c | E | SSRF / open redirect | accept | **`getInternalAppBaseUrl()`** only reaches self — same as Phase 3 | 
| T-04-04-d | I | Cross-doc | mitigate | **`after()`** passes **`doc_id`** from trusted **`runEmbedBatch`** result only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-04-01: scheduleEmbedBatchesForDoc + parse handshake</name>
  <files>src/lib/ingest/trigger-parse-batch.ts, src/lib/ingest/parse-document-batch.ts</files>
  <read_first>
    - src/lib/ingest/trigger-parse-batch.ts
    - src/lib/ingest/parse-document-batch.ts (lines 250–272)
    - src/app/api/internal/embed-batch/route.ts (Plan 04-03 output)
  </read_first>
  <acceptance_criteria>
    - **`export function scheduleEmbedBatchesForDoc(docId: string): void`** in **`trigger-parse-batch.ts`** (or **`trigger-ingest.ts`** if refactored — prefer **extend existing file** per **04-PATTERNS.md**)
    - Implementation mirrors **`scheduleParseBatchesForDoc`**: **`if (process.env.NODE_ENV === 'test') return`**, **`after(async () => fetch(`${base}/api/internal/embed-batch`, { POST, Authorization: Bearer, body: JSON.stringify({ doc_id }) }))`**
    - **`parse-document-batch.ts`**: immediately **after** successful **`update`** **`status: 'embedding'`** (same block where **`done: true`** returned), call **`scheduleEmbedBatchesForDoc(docId)`**
    - **`grep -n scheduleEmbedBatchesForDoc src/lib/ingest/parse-document-batch.ts`** shows call site
  </acceptance_criteria>
  <action>
    Add import of **`scheduleEmbedBatchesForDoc`** to **`parse-document-batch.ts`**. Avoid circular imports: if circular, move schedulers to **`trigger-ingest.ts`** re-export — prefer **no refactor** unless necessary.
  </action>
  <verify>
    <automated>pnpm typecheck && rg -n "scheduleEmbedBatchesForDoc|/api/internal/embed-batch" src/lib/ingest/trigger-parse-batch.ts src/lib/ingest/parse-document-batch.ts</automated>
  </verify>
  <done>Parse→embedding transition triggers first embed batch via after()</done>
</task>

<task type="auto">
  <name>Task 04-04-02: embed-batch route — chain when partial</name>
  <files>src/app/api/internal/embed-batch/route.ts</files>
  <read_first>
    - src/app/api/internal/parse-batch/route.ts (scheduleParseBatchesForDoc on !done)
    - src/app/api/internal/embed-batch/route.ts
  </read_first>
  <acceptance_criteria>
    - After **`runEmbedBatch`**, if **`!result.done`**, call **`scheduleEmbedBatchesForDoc(docId)`** — parallel **`parse-batch`** behavior
    - **`import { scheduleEmbedBatchesForDoc } from '@/lib/ingest/trigger-parse-batch'`** (path per repo alias)
  </acceptance_criteria>
  <action>
    Match **`parse-batch`** lines **82–84** pattern.
  </action>
  <verify>
    <automated>rg -n "scheduleEmbedBatchesForDoc|runEmbedBatch" src/app/api/internal/embed-batch/route.ts</automated>
  </verify>
  <done>Partial embed batches self-schedule</done>
</task>

<task type="auto">
  <name>Task 04-04-03: Vercel Cron + .env.example</name>
  <files>vercel.json, .env.example</files>
  <read_first>
    - vercel.json
    - .env.example
  </read_first>
  <acceptance_criteria>
    - **`vercel.json`** **`crons`** array contains **second** entry: **`"path": "/api/internal/embed-batch"`**, **`"schedule": "* * * * *"`** (same cadence as parse; executor verifies Vercel allows multiple crons)
    - **`.env.example`** comment block: **Vercel Cron** URL must include **`?secret=<INTERNAL_PARSE_SECRET>`** for **embed-batch** (cannot send **Authorization** header) — parallel existing parse-batch comment if present
  </acceptance_criteria>
  <action>
    Merge **`crons`** without removing **`parse-batch`** entry.
  </action>
  <verify>
    <automated>node -e "const j=require('./vercel.json'); console.log(j.crons.map(c=>c.path).join(','))"</automated>
  </verify>
  <done>Two crons: parse + embed</done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test`
</verification>

<success_criteria>
- Embedding pipeline self-schedules like parsing
- Cron hits embed worker in production configs
</success_criteria>

<output>
After completion, create `.planning/phases/04-embeddings-vector-store/04-04-SUMMARY.md`
</output>
