---
phase: 04-embeddings-vector-store
plan: 05
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-04"
files_modified:
  - src/lib/rag/match-document-chunks.ts
  - src/lib/rag/match-document-chunks.test.ts
  - src/app/api/internal/embed-batch/embed-batch.test.ts
  - scripts/smoke-vector-perf.ts
  - .planning/phases/04-embeddings-vector-store/04-UAT.md
autonomous: true
requirements:
  - INGEST-06
must_haves:
  truths:
    - "match_document_chunks RPC invoked only via supabaseAdmin + embed helper for query vector (D4-06)"
    - "Vitest covers RPC contract + embed-batch 401 without secret (optional route test)"
    - "04-UAT.md documents HNSW smoke &lt;500ms target and similarity sanity; roadmap success criteria 1–3 checked off with evidence"
  artifacts:
    - path: "src/lib/rag/match-document-chunks.ts"
      provides: "matchChunksForQuestion(docId, queryText) → top-K rows with page + content"
    - path: "scripts/smoke-vector-perf.ts"
      provides: "10k-vector seed + timing instructions"
    - path: ".planning/phases/04-embeddings-vector-store/04-UAT.md"
      provides: "UAT record with measured ms"
  key_links:
    - from: "match-document-chunks.ts"
      to: "public.match_document_chunks"
      via: "supabaseAdmin.rpc"
      pattern: "match_document_chunks"
---

<objective>
Add **server-only RAG retrieval wrapper** (**D4-06**): embed query text with **`text-embedding-004`**, call **`match_document_chunks`**, return top-5 citation rows. Add **Vitest** for RPC args + **`scripts/smoke-vector-perf.ts`** (or equivalent) for **10k-vector** HNSW timing. Author **`.planning/phases/04-embeddings-vector-store/04-UAT.md`** with **ROADMAP Phase 4 success criteria** sign-off notes.

Purpose: Prove **INGEST-06** + **D4-08** verification alignment before Phase 10 chat.

Output: Typed **`match_document_chunks`** caller, tests, perf script, UAT doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/database.types.ts (`Functions` after Plan 04-01)
@src/lib/embed/gemini-embed.ts
@src/db/client.ts
@.planning/ROADMAP.md (Phase 4 success criteria)
</context>

## Source coverage (this plan)

| Source | Items | Covered |
|--------|-------|---------|
| GOAL | Top-5 similarity + metadata | RPC wrapper + UAT |
| REQ INGEST-06 | Embeds + metadata | End-to-end verification section |
| CONTEXT | D4-08 tests | Vitest + UAT |
| ROADMAP | Success 1–3 | **verification** + **must_haves** |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAG helper → Postgres RPC | **Service role** only; **`doc_id`** from trusted server context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-a | I | Cross-doc data leak via RPC | mitigate | Wrapper **requires `docId: string`** (UUID); passes **`p_doc_id`**; **never** accepts client-provided embedding without `docId` pairing in same request |
| T-04-05-b | I | RPC exposed to anon | mitigate | **No HTTP route** for raw RPC in this plan; future chat API uses server-only helper + **`supabaseAdmin`** per **`src/db/client.ts`** |
| T-04-05-c | I | Query text logged | mitigate | Do not log full user query in production path; UAT may log locally |
| T-04-05-d | E | Service role misuse | mitigate | **`import "server-only"`** on **`match-document-chunks.ts`**; mirror **`src/db/client.ts`** pattern |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-05-01: match-document-chunks server helper</name>
  <files>src/lib/rag/match-document-chunks.ts</files>
  <read_first>
    - src/db/database.types.ts (Functions.match_document_chunks after codegen)
    - src/lib/embed/gemini-embed.ts (single-query embed: add **`embedQueryText(text: string)`** if missing — **768** dims)
    - supabase/migrations/20260508120000_phase4_hnsw_match_document_chunks.sql (Plan 04-01 file name)
  </read_first>
  <acceptance_criteria>
    - **`import "server-only"`** module **`src/lib/rag/match-document-chunks.ts`**
    - Exported async **`matchDocumentChunks(args: { docId: string; query: string; matchCount?: number })`** returns array of **`{ id, content, page_number, source_page_start, source_page_end, section, chunk_type, distance }`** aligned with RPC columns (exact names follow generated types)
    - Implementation: **`const q = await embedQueryText(query)`** (or **`embedContents`** of length 1) → **`supabaseAdmin.rpc('match_document_chunks', { p_doc_id: docId, p_query_embedding: …, p_match_count: matchCount ?? 5 })`**
    - **Vector format**: pass **`pgvector` string** or **`number[]`** per Supabase-js requirements for **`vector(768)`** — must match working **`update`** pattern from **Plan 04-03**
    - On RPC error: **throw** with message — caller (Phase 10) maps to user-safe errors
    - **`grep -n 'match_document_chunks' src/lib/rag/match-document-chunks.ts`**
  </acceptance_criteria>
  <action>
    **D4-08 (2):** Sample natural-language **`query`** → RPC returns **top-5** with **`page_number`** + **`content`** present in rows.
  </action>
  <verify>
    <automated>pnpm typecheck && rg -n "match_document_chunks|server-only" src/lib/rag/match-document-chunks.ts</automated>
  </verify>
  <done>Typed server wrapper calls RPC with doc_id filter</done>
</task>

<task type="auto">
  <name>Task 04-05-02: Vitest — RAG + optional route auth</name>
  <files>src/lib/rag/match-document-chunks.test.ts, src/app/api/internal/embed-batch/route.ts</files>
  <read_first>
    - src/lib/rag/match-document-chunks.ts
    - src/lib/ingest/parse-document-batch.test.ts
  </read_first>
  <acceptance_criteria>
    - **`match-document-chunks.test.ts`**: mock **`supabaseAdmin.rpc`** + **`embedQueryText`** / **`embedContents`** — assert **`p_doc_id`** equals test UUID and **`p_match_count <= 50`**
    - Optional (recommended): **`src/app/api/internal/embed-batch/embed-batch.test.ts`** or colocated test: **`GET /api/internal/embed-batch`** without secret → **401** — use **`import { GET } from '@/app/api/internal/embed-batch/route'`** + **`new Request(...)`** if viable in Vitest
    - **`pnpm test`** passes offline
  </acceptance_criteria>
  <action>
    Keep tests **fully mocked** — no **`GEMINI_API_KEY`** required in CI.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/rag/match-document-chunks.test.ts</automated>
  </verify>
  <done>Automated contract tests for RAG helper</done>
</task>

<task type="auto">
  <name>Task 04-05-03: smoke-vector-perf + 04-UAT.md</name>
  <files>scripts/smoke-vector-perf.ts, .planning/phases/04-embeddings-vector-store/04-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 success criteria)
    - package.json (tsx? — use **`npx tsx`** or **`node --import tsx`** if configured; else **plain SQL** in **`scripts/smoke-vector-perf.sql`**)
  </read_first>
  <acceptance_criteria>
    - **`scripts/smoke-vector-perf.ts`** (or **`.sql`**) documents: insert **10,000** rows into **`chunks`** for a **throwaway `doc_id`** with **random normalized** **`vector(768)`** embeddings (local/dev only), run **`ANALYZE chunks`**, time **one** **`match_document_chunks`** call (or raw SQL **`ORDER BY embedding <=> query`**) — executor records **elapsed ms**
    - **`.planning/phases/04-embeddings-vector-store/04-UAT.md`** includes sections:
      - **S1**: Every chunk has **non-null `embedding`** after successful embed run (**D4-08**)
      - **S2**: Query **"What was the net income in 2023?"** (or similar) → RPC top-5 with **`page_number`** + readable **`content`**
      - **S3**: **10k** smoke result **&lt; 500ms** (or documented hardware-adjusted figure per **D4-08**)
    - Script header comment: **destructive to dev DB** — run **`supabase db reset`** after if needed
  </acceptance_criteria>
  <action>
    Use **service-role** connection only in local script via env vars documented in script — **never** commit secrets. If **`tsx`** not in repo, use **`pnpm exec supabase db execute -f ...`** for **SQL-only** path.
  </action>
  <verify>
    <automated>test -f scripts/smoke-vector-perf.ts || test -f scripts/smoke-vector-perf.sql; test -f .planning/phases/04-embeddings-vector-store/04-UAT.md</automated>
  </verify>
  <done>UAT doc + perf artifact exist</done>
</task>

</tasks>

<verification>
- `pnpm test`
- `pnpm typecheck`
- `pnpm lint`
- **ROADMAP Phase 4 success criteria (must pass for phase sign-off):**
  1. After ingestion completes, every chunk has **`embedding`** populated (and citation columns unchanged) — **stub path excluded** (**`STUB_PIPELINE_TICK`** off for real validation) per **D4-07**.
  2. Similarity search returns **top-5** with **`page_number`** intact and readable **`content`** (**D4-08**).
  3. **HNSW** active; **10k-vector** smoke **&lt; 500ms** documented in **04-UAT.md** (**D4-08**).
</verification>

<success_criteria>
- **D4-01–D4-08** behaviors verifiable from tests + UAT notes
- **Phase 4 roadmap** truths **1–3** evidenced in **04-UAT.md**
</success_criteria>

<output>
After completion, create `.planning/phases/04-embeddings-vector-store/04-05-SUMMARY.md`
</output>
