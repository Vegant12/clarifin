---
phase: 04-embeddings-vector-store
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/embed/gemini-embed.ts
  - src/lib/embed/gemini-embed.test.ts
autonomous: true
requirements:
  - INGEST-06
must_haves:
  truths:
    - "Server-only module embeds strings with model text-embedding-004 (768 dims) using GEMINI_API_KEY per D4-01"
    - "Input texts are chunks.content only (caller responsibility per D4-02); helper exposes batch API for D4-04"
    - "429/transient errors use bounded exponential backoff; throws after cap (caller maps to document failed)"
  artifacts:
    - path: "src/lib/embed/gemini-embed.ts"
      provides: "embedTextBatch + dimension constant 768"
    - path: "src/lib/embed/gemini-embed.test.ts"
      provides: "Mocked HTTP / client — no live Gemini in pnpm test"
  key_links:
    - from: "gemini-embed.ts"
      to: "embed-document-batch.ts (Plan 04-03)"
      via: "import embed helper"
      pattern: "from \"@/lib/embed/gemini-embed\""
---

<objective>
Implement **`text-embedding-004`** embedding helper (**768** dimensions) using **`GEMINI_API_KEY`** (**D4-01**, **D4-02**, **D4-04**). Prefer **`@google/genai`** (already in `package.json`) or **Generative Language REST** with **batched** requests; no **`NEXT_PUBLIC_*`** secrets.

Purpose: Isolate Google API quirks, batching, and retries so **Plan 04-03** orchestration stays small.

Output: `src/lib/embed/gemini-embed.ts` + Vitest suite with mocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/env.ts (`GEMINI_API_KEY`)
@.planning/phases/04-embeddings-vector-store/04-PATTERNS.md
@.planning/phases/04-embeddings-vector-store/04-RESEARCH.md § Google Embedding API
</context>

## Source coverage (this plan)

| Source | Items | Covered by tasks |
|--------|-------|------------------|
| CONTEXT | D4-01, D4-02, D4-04 | Model, content-only contract, batch+retry |
| RESEARCH | Batch + 429 backoff | Task implementation |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GEMINI_API_KEY` | Server-only; never exposed to client bundles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-a | I | Gemini API key | mitigate | Read **`env.GEMINI_API_KEY`** only inside **`import "server-only"`** module; **never** `process.env.NEXT_PUBLIC_*` |
| T-04-02-b | I | Unauthenticated embed trigger | mitigate | This module **exports no route** — only imported by server orchestrator (**D4-03** in Plan 04-03) |
| T-04-02-c | S | Key in logs | mitigate | Do **not** log API key; on error log status code only |
| T-04-02-d | E | Rate limit / quota | mitigate | **Bounded backoff** on **429** per **D4-04**; surface throw after max retries |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 04-02-01: gemini-embed.ts server helper</name>
  <files>src/lib/embed/gemini-embed.ts</files>
  <read_first>
    - src/lib/env.ts
    - package.json (@google/genai version)
    - .planning/phases/04-embeddings-vector-store/04-CONTEXT.md (D4-01–D4-04)
  </read_first>
  <acceptance_criteria>
    - File starts with **`import "server-only"`**
    - Exported constant **`EMBEDDING_MODEL_ID = "text-embedding-004"`** and **`EMBEDDING_DIMENSIONS = 768`**
    - Primary export: async **`embedTextBatch(texts: string[]): Promise<number[][]>`** (or equivalent name) where each inner array has **exactly 768** finite numbers
    - Uses **`GEMINI_API_KEY`** from **`@/lib/env`** (validated at load) — **D4-01**
    - Batching: single RPC to Google with **multiple texts per request** where API allows (**batchEmbedContents**-style); if SDK only supports single, implement **`EMBED_TEXTS_BATCH_SIZE`** constant (e.g. **100** or official max) and chunk **`texts`** loop with sequential calls — document chosen limit with comment citing Google docs snapshot
    - **D4-04**: on **429** or **5xx**, **`await sleep`** with exponential backoff (**max 4** retries, base **200ms**, cap **10s**); rethrow last error if exhausted
    - Empty input returns **`[]`** without calling API
    - **`rg "NEXT_PUBLIC" src/lib/embed/gemini-embed.ts`** returns **no** matches (empty output; exit 1 acceptable)
  </acceptance_criteria>
  <action>
    1. Implement using **`@google/genai`** **`GoogleGenAI`** embeddings API **or** REST **`https://generativelanguage.googleapis.com/v1beta/models/text-embedding-004:batchEmbedContents`** — align with existing repo patterns if **`@google/genai`** already used elsewhere (**grep** first).
    2. Normalize API response to **`number[][]`** aligned 1:1 with input **`texts`** order (failure if count mismatch).
    3. No section prefix (**D4-02** default off) — orchestrator passes **`chunks.content` only** in Plan 04-03.
  </action>
  <verify>
    <automated>pnpm typecheck && rg -n "server-only|EMBEDDING_DIMENSIONS|768" src/lib/embed/gemini-embed.ts</automated>
  </verify>
  <done>Server-only embed helper compiles; constants match D4-01</done>
</task>

<task type="auto">
  <name>Task 04-02-02: Vitests — mocked API</name>
  <files>src/lib/embed/gemini-embed.test.ts</files>
  <read_first>
    - src/lib/ingest/parse-document-batch.test.ts (mock patterns)
    - src/lib/embed/gemini-embed.ts
  </read_first>
  <acceptance_criteria>
    - **`pnpm test`** runs **`src/lib/embed/gemini-embed.test.ts`** with **no network** (mock **`@google/genai`** or **`global.fetch`**)
    - Test: **two** input strings return **two** vectors length **768**
    - Test: **429** once then success exercises retry path (or retry helper unit)
    - Test: empty array returns **[]
  </acceptance_criteria>
  <action>
    Mock Gemini so determinism: e.g. **768** floats **0..1** from hash of string. Assert dimension and count.
  </action>
  <verify>
    <automated>pnpm test -- src/lib/embed/gemini-embed.test.ts</automated>
  </verify>
  <done>Vitest passes without live Gemini</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/lib/embed/gemini-embed.test.ts`
- `pnpm typecheck`
- `pnpm lint`
</verification>

<success_criteria>
- Deterministic mocked tests for embed helper
- **768**-dim contract enforced
</success_criteria>

<output>
After completion, create `.planning/phases/04-embeddings-vector-store/04-02-SUMMARY.md`
</output>
